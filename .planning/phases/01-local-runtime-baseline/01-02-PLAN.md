---
phase: 01-local-runtime-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-04"
files_modified:
  - src/aurora/runtime/model_source.py
  - src/aurora/runtime/model_download.py
  - src/aurora/runtime/model_registry.py
  - src/aurora/cli/model.py
  - tests/runtime/test_model_source.py
  - tests/runtime/test_model_download.py
  - tests/cli/test_model_command.py
autonomous: true
requirements:
  - MOD-01
  - PRIV-01
must_haves:
  truths:
    - "User can configure local llama.cpp endpoint and model via `aurora model set`."
    - "Hugging Face source input accepts `repo/model:arquivo.gguf` and prioritizes local cache when available."
    - "Large model downloads require explicit confirmation and support private-token prompt flow."
  artifacts:
    - path: "src/aurora/cli/model.py"
      provides: "`aurora model set` command with config persistence"
      exports: ["model_set_command"]
    - path: "src/aurora/runtime/model_source.py"
      provides: "HF target parser and validation helpers"
      contains: "parse_hf_target"
    - path: "src/aurora/runtime/model_download.py"
      provides: "Download orchestration with progress/retry/offline handling"
      contains: "download_model"
  key_links:
    - from: "src/aurora/cli/model.py"
      to: "src/aurora/runtime/model_source.py"
      via: "source parsing before save/download"
      pattern: "parse_hf_target"
    - from: "src/aurora/cli/model.py"
      to: "src/aurora/runtime/model_download.py"
      via: "download pipeline with confirmation and retries"
      pattern: "download_model"
    - from: "src/aurora/cli/model.py"
      to: "src/aurora/runtime/settings.py"
      via: "persist endpoint/model/source globally"
      pattern: "save_settings"
---

<objective>
Implement the model-configuration command flow that lets users define local llama.cpp endpoint/model settings and optionally resolve/download models from Hugging Face with safe prompts.

Purpose: Deliver the command-based side of setup UX required in Phase 1 and make model configuration reliable before wizard integration.
Output: Working `aurora model set` command plus model source/download runtime helpers and tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-local-runtime-baseline/01-RESEARCH.md
@.planning/phases/01-local-runtime-baseline/01-01-SUMMARY.md
@.planning/phases/01-local-runtime-baseline/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Hugging Face model source parsing and cache-aware resolution</name>
  <files>src/aurora/runtime/model_source.py, src/aurora/runtime/model_registry.py, tests/runtime/test_model_source.py</files>
  <behavior>
    - `repo/model:arquivo.gguf` input parses into `repo_id` and `filename`.
    - Invalid source formats return actionable pt-BR validation errors.
    - If model file already exists in Aurora global model directory, cache is preferred over remote fetch.
  </behavior>
  <action>Create model source utilities to validate and parse Hugging Face targets in the locked `repo/model:arquivo.gguf` format. Implement a model registry helper that checks the global Aurora model directory before any download attempt and returns deterministic local path metadata. Ensure parse/validation errors are grouped and actionable so command UX can print direct recovery instructions.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_model_source.py -q</automated>
  </verify>
  <done>Model source parser and cache resolver are reusable by both command and wizard flows and fully covered by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resilient model download pipeline with token, progress, and retry guidance</name>
  <files>src/aurora/runtime/model_download.py, tests/runtime/test_model_download.py</files>
  <behavior>
    - Public and private model downloads are supported with secure token input handling.
    - Download output includes percent, size, and ETA progress details.
    - Offline/auth/download failures produce guided retry instructions instead of generic tracebacks.
  </behavior>
  <action>Implement download orchestration using `huggingface_hub` with support for private models, offline detection, and retryable failure categories. Require explicit user confirmation for large downloads before network transfer starts. Ensure output formatting exposes progress (percent/size/ETA) and keeps sensitive token/path data masked. Add tests that cover confirmation gates, offline failure guidance, token-required flows, and cache short-circuit behavior.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_model_download.py -q</automated>
  </verify>
  <done>Download pipeline is deterministic, secure, and user-guided for all expected Phase 1 HF setup paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver `aurora model set` command integrated with global config and local-only policy</name>
  <files>src/aurora/cli/model.py, tests/cli/test_model_command.py</files>
  <behavior>
    - Command sets endpoint/model/source globally and keeps previously downloaded models available.
    - Cloud endpoints are blocked in Phase 1 with clear explanation and recovery command.
    - On successful update, command prints next action guidance for runtime validation.
  </behavior>
  <action>Implement `aurora model set` command with options for endpoint URL, model id, and HF source. Wire to settings persistence and policy validation from Plan 04, so cloud endpoints fail fast with actionable pt-BR messaging. Integrate model source parser and download pipeline to support direct model setup from Hugging Face, including explicit confirmation for large downloads and retry hints on failures. Ensure command leaves existing downloaded models intact and only switches default selection.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_model_command.py -q</automated>
  </verify>
  <done>`aurora model set` fully configures local model runtime state and handles HF flows required by Phase 1 without violating local-only policy.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_model_source.py tests/runtime/test_model_download.py tests/cli/test_model_command.py -q` passes.
- `uv run aurora model set --help` documents endpoint/model/source options and confirmation behavior.
- Running `aurora model set` with a non-local endpoint is blocked by local-only policy messaging.
</verification>

<success_criteria>
- Command-mode model configuration works for local llama.cpp endpoint and model defaults (MOD-01).
- HF source handling supports parse, cache-prioritization, download confirmation, and retry guidance.
- Local-only guardrails remain enforced during model setup flows (PRIV-01).
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-baseline/01-02-SUMMARY.md`
</output>
