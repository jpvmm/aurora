---
phase: 01-local-runtime-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - src/aurora/__init__.py
  - src/aurora/cli/__init__.py
  - src/aurora/cli/app.py
  - tests/cli/test_entrypoint.py
autonomous: true
requirements:
  - CLI-01
must_haves:
  truths:
    - "User can install Aurora as a global CLI tool and invoke `aurora` from any directory."
    - "Root command exposes the Phase 1 command groups (`setup`, `config`, `model`, `doctor`) with explicit placeholder behavior."
  artifacts:
    - path: "pyproject.toml"
      provides: "Global CLI entrypoint via [project.scripts]"
      contains: "aurora = \"aurora.cli.app:app\""
    - path: "src/aurora/cli/app.py"
      provides: "Typer root app that registers command groups"
      exports: ["app"]
    - path: "tests/cli/test_entrypoint.py"
      provides: "CLI smoke checks for entrypoint and command-group discovery"
      contains: "--help assertions"
  key_links:
    - from: "pyproject.toml"
      to: "src/aurora/cli/app.py"
      via: "project script entrypoint"
      pattern: "aurora = \"aurora.cli.app:app\""
    - from: "src/aurora/cli/app.py"
      to: "tests/cli/test_entrypoint.py"
      via: "help snapshot assertions for registered command groups"
      pattern: "setup/config/model/doctor"
---

<objective>
Create the CLI/package baseline for Phase 1 so Aurora can be installed globally and expose the required command surface from any terminal folder.

Purpose: Establish the non-negotiable invocation contract (`aurora`) and command-group skeleton that later runtime and setup plans will implement.
Output: Installable Python CLI scaffold with root app, stub command groups, and entrypoint smoke tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-local-runtime-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap package metadata, root CLI app, and entrypoint smoke tests</name>
  <files>pyproject.toml, src/aurora/__init__.py, src/aurora/cli/__init__.py, src/aurora/cli/app.py, tests/cli/test_entrypoint.py</files>
  <behavior>
    - `uv run aurora --help` resolves the root command after local install/sync.
    - Root CLI app is created with Typer and prepared to register Phase 1 command groups.
    - Entry-point tests validate script resolution and help output stability.
  </behavior>
  <action>Initialize the Python package scaffold under `src/aurora`, define `[project.scripts] aurora = "aurora.cli.app:app"` in `pyproject.toml`, and implement a Typer root command object in `src/aurora/cli/app.py`. Ensure the root app is importable and testable through the project script. Add CLI smoke tests for entrypoint resolution and base help rendering.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_entrypoint.py -q</automated>
  </verify>
  <done>Global CLI entrypoint resolves locally, and root help output is test-covered and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Phase 1 command groups as explicit root placeholders and document install flow</name>
  <files>README.md, src/aurora/cli/app.py, tests/cli/test_entrypoint.py</files>
  <behavior>
    - Root `--help` lists `setup`, `config`, `model`, and `doctor` groups.
    - Group callbacks return explicit "not implemented yet" errors in pt-BR.
    - README documents global install and PATH-recovery commands for `uv tool` and `pipx`.
  </behavior>
  <action>Define and register the `setup`, `config`, `model`, and `doctor` Typer sub-app placeholders directly in `src/aurora/cli/app.py` so the command surface is visible without creating additional command modules in this plan. Keep placeholder behavior explicit in pt-BR (no silent no-op) and extend entrypoint smoke tests to assert command-group discoverability. Update `README.md` with global installation instructions (`uv tool install .`, `pipx install .`) and PATH troubleshooting (`uv tool update-shell`, `pipx ensurepath`). Keep command naming aligned with the locked decisions in `01-CONTEXT.md`.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_entrypoint.py -q && rg -n "uv tool install \\.|pipx install \\.|uv tool update-shell|pipx ensurepath" README.md</automated>
  </verify>
  <done>Required command groups are discoverable with explicit placeholder behavior, and install/troubleshooting guidance is documented.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/cli/test_entrypoint.py -q` passes.
- `uv run aurora --help` shows `setup`, `config`, `model`, and `doctor`.
- `README.md` includes global install and PATH troubleshooting commands for both `uv tool` and `pipx`.
</verification>

<success_criteria>
- CLI global entrypoint contract for `aurora` is implemented and test-covered (CLI-01).
- Command surface skeleton is available for runtime setup/model plans without renaming churn.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-baseline/01-01-SUMMARY.md`
</output>
