---
phase: 01-local-runtime-baseline
plan: 05
type: execute
gap_closure: true
wave: 4
depends_on:
  - "01-01"
  - "01-03"
files_modified:
  - tests/cli/test_entrypoint.py
  - src/aurora/cli/app.py
autonomous: true
requirements:
  - CLI-01
  - MOD-01
  - MOD-03
  - PRIV-01
  - PRIV-04
must_haves:
  truths:
    - "Entrypoint smoke tests reflect implemented Phase 1 behavior for `setup`, `config`, `model`, and `doctor` command groups."
    - "`uv run pytest tests/cli/test_entrypoint.py -q` passes with no legacy placeholder assertions for setup/config/doctor."
    - "Root command help still exposes the complete Phase 1 command surface required by runtime/privacy flows."
  artifacts:
    - path: "tests/cli/test_entrypoint.py"
      provides: "Updated smoke-test contract aligned to implemented command behavior"
      contains: "help/discovery assertions without setup/config/doctor placeholder checks"
    - path: "src/aurora/cli/app.py"
      provides: "Root CLI group wiring to implemented command modules"
      exports: ["app"]
  key_links:
    - from: "src/aurora/cli/app.py"
      to: "tests/cli/test_entrypoint.py"
      via: "root command-group registration and behavior assertions"
      pattern: "setup/config/model/doctor"
---

<objective>
Close the Phase 1 verification regression by updating root entrypoint smoke expectations to match the implemented command behavior.

Purpose: Restore green CLI smoke coverage without reopening completed feature scope, while preserving the command surface that supports Phase 1 model/runtime/privacy requirements.
Output: Targeted test-contract updates (and minimal root wiring adjustments only if needed) so entrypoint verification is accurate and stable.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-local-runtime-baseline/01-VERIFICATION.md
@.planning/phases/01-local-runtime-baseline/01-01-PLAN.md
@.planning/phases/01-local-runtime-baseline/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebaseline entrypoint smoke tests to implemented command behavior</name>
  <files>tests/cli/test_entrypoint.py</files>
  <behavior>
    - Smoke tests validate command discovery/help for `setup`, `config`, `model`, and `doctor`.
    - Tests no longer require legacy `ainda não implementado` placeholders for setup/config/doctor.
    - Assertions remain deterministic and fast for CI/phase verification.
  </behavior>
  <action>Update `tests/cli/test_entrypoint.py` to remove stale placeholder assertions tied to Plan 01 scaffolding and assert current implemented behavior from later plans. Keep the scope strictly on entrypoint contract coverage: command presence, help rendering, and non-placeholder command execution expectations where appropriate.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_entrypoint.py -q</automated>
  </verify>
  <done>Entrypoint smoke tests are aligned with current CLI behavior and pass reliably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden root CLI wiring only where test rebaseline reveals mismatch</name>
  <files>src/aurora/cli/app.py, tests/cli/test_entrypoint.py</files>
  <behavior>
    - Root `aurora --help` continues to expose `setup`, `config`, `model`, and `doctor`.
    - `setup`/`config`/`doctor` are wired to real implementations, not fallback placeholders.
    - Updated smoke tests pass together with setup/config/doctor command tests.
  </behavior>
  <action>Audit `src/aurora/cli/app.py` command-group registration against the updated smoke-test contract. If mismatches exist, apply minimal wiring fixes (no feature expansion) so root command behavior stays consistent with implemented modules from Plans 02-04. Keep changes narrowly focused on regression closure and avoid modifying completed plan scope.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_entrypoint.py tests/cli/test_setup_wizard.py tests/cli/test_config_show.py tests/cli/test_doctor.py -q</automated>
  </verify>
  <done>Root command contract is coherent with implemented modules and guarded by passing CLI regression tests.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/cli/test_entrypoint.py -q` passes.
- `uv run pytest tests/cli/test_setup_wizard.py tests/cli/test_config_show.py tests/cli/test_doctor.py -q` passes.
- Entrypoint tests no longer encode obsolete placeholder behavior for setup/config/doctor commands.
</verification>

<success_criteria>
- Phase 1 verification gap about stale placeholder smoke assertions is closed.
- CLI entrypoint contract remains stable for setup/model/config/doctor surfaces needed by CLI-01, MOD-01, MOD-03, PRIV-01, and PRIV-04.
- Gap closure remains targeted: no new feature scope beyond regression correction.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-baseline/01-05-SUMMARY.md`
</output>
