---
phase: 01-local-runtime-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aurora/runtime/paths.py
  - src/aurora/runtime/settings.py
  - src/aurora/privacy/policy.py
  - tests/runtime/test_settings_defaults.py
  - tests/privacy/test_policy.py
autonomous: true
requirements:
  - PRIV-01
  - PRIV-04
must_haves:
  truths:
    - "Aurora persists global runtime settings in a per-user location independent of current working directory."
    - "Settings defaults enforce local-only mode and telemetry disabled in Phase 1."
    - "Cloud endpoint values are rejected by policy checks before runtime calls."
  artifacts:
    - path: "src/aurora/runtime/settings.py"
      provides: "Typed settings load/save with privacy defaults"
      contains: "local_only=True, telemetry_enabled=False"
    - path: "src/aurora/privacy/policy.py"
      provides: "Endpoint allowlist and telemetry policy guards"
      contains: "localhost/127.0.0.1 enforcement"
    - path: "tests/runtime/test_settings_defaults.py"
      provides: "Automated defaults/path/persistence checks"
      contains: "round-trip assertions"
  key_links:
    - from: "src/aurora/runtime/settings.py"
      to: "src/aurora/runtime/paths.py"
      via: "global config path resolution"
      pattern: "platformdirs"
    - from: "src/aurora/runtime/settings.py"
      to: "src/aurora/privacy/policy.py"
      via: "endpoint validation on save"
      pattern: "validate_local_endpoint"
---

<objective>
Create the runtime privacy foundation for Phase 1 with global typed settings and local-only policy enforcement.

Purpose: Isolate privacy/runtime contracts into a focused plan so downstream model and wizard plans depend on stable settings and policy modules without overloading the CLI bootstrap plan.
Output: Per-user settings path utilities, typed settings load/save APIs, endpoint policy guards, and targeted tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-local-runtime-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement global typed settings persistence with privacy-first defaults</name>
  <files>src/aurora/runtime/paths.py, src/aurora/runtime/settings.py, tests/runtime/test_settings_defaults.py</files>
  <behavior>
    - Settings are persisted in a per-user global config directory and do not depend on current working directory.
    - Default endpoint/model values are present, with `local_only=true` and telemetry disabled by default.
    - Loading/saving settings remains deterministic across multiple invocations.
  </behavior>
  <action>Implement settings persistence using `platformdirs` + `pydantic-settings`, including fields for endpoint URL, model id, model source, local-only mode, and telemetry flags. Keep persisted scope global-per-user and set default model to `Qwen3-8B-Q8_0`. Provide explicit load/save APIs reusable by `model set`, `setup`, and `config show` flows in later plans.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_settings_defaults.py -q</automated>
  </verify>
  <done>Global settings API exists with stable serialization and required Phase 1 defaults for local-only runtime and telemetry-off policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local-only endpoint policy enforcement and telemetry guardrails</name>
  <files>src/aurora/privacy/policy.py, src/aurora/runtime/settings.py, tests/privacy/test_policy.py</files>
  <behavior>
    - Loopback hosts (`localhost`, `127.0.0.1`) pass local-only validation.
    - Non-local endpoints are blocked with actionable pt-BR errors in Phase 1.
    - Telemetry defaults remain visible through settings helpers for future config/status commands.
  </behavior>
  <action>Create policy utilities that classify endpoint URLs and block cloud hosts when `local_only` is active (Phase 1 default). Integrate policy validation into settings save/update paths so invalid endpoints are rejected before runtime calls. Define canonical telemetry-off defaults (`AGNO_TELEMETRY=false`, `GRAPHITI_TELEMETRY_ENABLED=false`) in settings helpers for later display in `aurora config show`.</action>
  <verify>
    <automated>uv run pytest tests/privacy/test_policy.py tests/runtime/test_settings_defaults.py -q</automated>
  </verify>
  <done>Policy module blocks cloud endpoints by default and settings layer enforces telemetry-off/local-only behavior required by Phase 1.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_settings_defaults.py tests/privacy/test_policy.py -q` passes.
- Saving settings with non-loopback endpoint returns blocked local-only policy error in pt-BR.
- Settings persistence remains global-per-user across different working directories.
</verification>

<success_criteria>
- Global settings defaults enforce local-only mode and telemetry disabled (PRIV-01, PRIV-04).
- Endpoint policy guardrails are enforced at persistence boundaries and test-covered.
- Runtime privacy contracts are available for model/setup/doctor plans without expanding Plan 01 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-baseline/01-04-SUMMARY.md`
</output>
