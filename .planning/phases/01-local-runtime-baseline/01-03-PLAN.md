---
phase: 01-local-runtime-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-04"
files_modified:
  - src/aurora/runtime/errors.py
  - src/aurora/runtime/llama_client.py
  - src/aurora/cli/setup.py
  - src/aurora/cli/config.py
  - src/aurora/cli/doctor.py
  - tests/runtime/test_llama_client.py
  - tests/cli/test_setup_wizard.py
  - tests/cli/test_config_show.py
  - tests/cli/test_doctor.py
autonomous: true
requirements:
  - MOD-01
  - MOD-03
  - PRIV-04
must_haves:
  truths:
    - "First run opens a guided setup wizard when required runtime config is missing."
    - "Wizard blocks completion until endpoint connectivity and active model validation pass."
    - "When runtime checks fail, users receive categorized pt-BR errors with exact recovery commands."
    - "Config/status output clearly shows local-only and telemetry-off defaults."
  artifacts:
    - path: "src/aurora/cli/setup.py"
      provides: "First-run guided setup flow"
      exports: ["setup_command", "run_first_run_wizard"]
    - path: "src/aurora/runtime/llama_client.py"
      provides: "Connectivity and model-presence probe client"
      contains: "/health and /v1/models checks"
    - path: "src/aurora/runtime/errors.py"
      provides: "Typed error taxonomy with actionable recovery mapping"
      contains: "endpoint_offline/model_missing/invalid_token/timeout"
    - path: "src/aurora/cli/config.py"
      provides: "`aurora config show` visibility for telemetry/local-only settings"
      exports: ["config_show_command"]
  key_links:
    - from: "src/aurora/cli/setup.py"
      to: "src/aurora/runtime/llama_client.py"
      via: "wizard completion gate"
      pattern: "validate_runtime"
    - from: "src/aurora/runtime/llama_client.py"
      to: "src/aurora/runtime/errors.py"
      via: "exception classification"
      pattern: "classify_runtime_error"
    - from: "src/aurora/cli/config.py"
      to: "src/aurora/runtime/settings.py"
      via: "display persisted privacy defaults"
      pattern: "load_settings"
---

<objective>
Finish Phase 1 user-facing runtime readiness by delivering the first-run wizard, runtime diagnostics, and actionable model connectivity errors with visible privacy defaults.

Purpose: Convert setup/config from static values into a validated operating flow that prevents broken runtime states and explains recovery in plain pt-BR.
Output: First-run setup wizard, runtime probe/error modules, and `config show`/`doctor` diagnostic commands.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-local-runtime-baseline/01-RESEARCH.md
@.planning/phases/01-local-runtime-baseline/01-01-SUMMARY.md
@.planning/phases/01-local-runtime-baseline/01-02-SUMMARY.md
@.planning/phases/01-local-runtime-baseline/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement runtime probe client and deterministic error classification</name>
  <files>src/aurora/runtime/errors.py, src/aurora/runtime/llama_client.py, tests/runtime/test_llama_client.py</files>
  <behavior>
    - Connectivity check distinguishes endpoint offline, timeout, and transitional model-loading states.
    - Model availability check validates selected model id against `/v1/models` payload.
    - Returned errors carry specific pt-BR recovery commands including `aurora doctor` when relevant.
  </behavior>
  <action>Implement `llama_client` probes for `/health` and `/v1/models` with explicit timeout strategy and retry behavior for transitional loading responses. Build typed runtime errors and a classifier that maps HTTP/network/auth failures into user-facing categories (`endpoint_offline`, `timeout`, `model_missing`, `invalid_token`). Each category must provide exact CLI remediation commands and avoid generic exception dumps.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_llama_client.py -q</automated>
  </verify>
  <done>Runtime validation layer can reliably determine endpoint and model readiness and emits actionable categorized errors for MOD-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build first-run setup wizard with blocking validation and summary output</name>
  <files>src/aurora/cli/setup.py, tests/cli/test_setup_wizard.py</files>
  <behavior>
    - Missing required config triggers guided wizard on first run.
    - Wizard supports local endpoint/model setup plus HF source path flow and uses `aurora model set` logic for download/config persistence.
    - Wizard only completes after successful runtime validation and prints next command guidance (`aurora ask`).
    - Onboarding summary explicitly shows how to change assistant language after setup (for example: `aurora config set language <codigo>`).
  </behavior>
  <action>Implement pt-BR setup wizard that collects endpoint, model id, and optional Hugging Face source; enforce Phase 1 local-only restrictions; and reuse model configuration helpers from Plan 02 so behavior is consistent between command and wizard paths. Block completion until runtime validation succeeds, and on failure provide grouped guidance with immediate retry/correction path. Final screen must show setup summary, local-only status, telemetry status, explicit guidance for changing language, and next-step command example.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_setup_wizard.py -q</automated>
  </verify>
  <done>First-run setup flow is guided, blocking, and self-recovering with explicit validation, language-switch guidance, and summary output in pt-BR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose diagnostics via `config show` and `doctor` with privacy-state visibility</name>
  <files>src/aurora/cli/config.py, src/aurora/cli/doctor.py, tests/cli/test_config_show.py, tests/cli/test_doctor.py</files>
  <behavior>
    - `aurora config show` displays endpoint/model/source and explicitly shows telemetry disabled state.
    - `aurora doctor` validates runtime dependencies and model readiness with actionable fixes.
    - Sensitive token/path output is masked in diagnostics.
  </behavior>
  <action>Implement `aurora config show` for human-readable global config inspection and ensure telemetry/local-only values are always explicit. Implement `aurora doctor` checks for endpoint reachability, model presence, and policy mismatches with grouped issue reporting and direct recovery commands. Keep output aligned with Phase 1 privacy UX: no sensitive-value leaks, clear local-only banner, and practical pt-BR wording.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_config_show.py tests/cli/test_doctor.py -q</automated>
  </verify>
  <done>User can validate and troubleshoot runtime setup end-to-end using built-in commands, while privacy defaults stay transparent and enforced.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_llama_client.py tests/cli/test_setup_wizard.py tests/cli/test_config_show.py tests/cli/test_doctor.py -q` passes.
- Running `uv run aurora` without required config starts wizard and blocks completion on failed validation.
- `uv run aurora config show` includes explicit local-only and telemetry-off status output.
- Wizard completion output includes explicit command guidance to change assistant language after onboarding.
</verification>

<success_criteria>
- Wizard and command paths both satisfy local model setup requirements with validation gate (MOD-01).
- Endpoint/model failures are classified and explained with exact recovery actions (MOD-03).
- Telemetry-disabled default is visible in runtime status/config outputs (PRIV-04).
- Setup experience defaults to pt-BR and explicitly teaches how to switch language (context compliance).
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-runtime-baseline/01-03-SUMMARY.md`
</output>
