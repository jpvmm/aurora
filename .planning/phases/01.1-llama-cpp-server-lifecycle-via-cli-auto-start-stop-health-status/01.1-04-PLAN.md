---
phase: 01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status
plan: 04
type: execute
wave: 4
depends_on:
  - "01.1-02"
  - "01.1-03"
files_modified:
  - src/aurora/runtime/server_lifecycle.py
  - src/aurora/cli/model.py
  - tests/runtime/test_server_lifecycle.py
  - tests/cli/test_model_command.py
autonomous: true
gap_closure: true
requirements:
  - CLI-02
  - CLI-04
  - MOD-03
  - PRIV-01
must_haves:
  truths:
    - "`aurora model health` payload always includes PID and uptime alongside endpoint/model/readiness fields."
    - "`aurora model health` text and `--json` outputs expose the same PID/uptime contract deterministically."
    - "Runtime and CLI regression tests fail if health payload/renderer drops PID or uptime fields."
  artifacts:
    - path: "src/aurora/runtime/server_lifecycle.py"
      provides: "LifecycleHealth contract with PID and uptime sourced from runtime status metadata"
      contains: "LifecycleHealth dataclass + check_health payload construction"
    - path: "src/aurora/cli/model.py"
      provides: "`model health` text/json rendering for PID and uptime contract fields"
      contains: "_render_health"
    - path: "tests/runtime/test_server_lifecycle.py"
      provides: "Runtime contract coverage for health payload PID/uptime fields"
      contains: "health assertions for running and error paths"
    - path: "tests/cli/test_model_command.py"
      provides: "CLI contract coverage for `model health` text/json payload fields"
      contains: "json and text output assertions for pid/uptime"
  key_links:
    - from: "src/aurora/runtime/server_lifecycle.py"
      to: "src/aurora/cli/model.py"
      via: "LifecycleHealth -> _render_health mapping for JSON/text contract"
      pattern: "check_health|LifecycleHealth|_render_health"
    - from: "tests/runtime/test_server_lifecycle.py"
      to: "src/aurora/runtime/server_lifecycle.py"
      via: "health payload contract assertions"
      pattern: "check_health"
    - from: "tests/cli/test_model_command.py"
      to: "src/aurora/cli/model.py"
      via: "CLI renderer contract assertions for `model health`"
      pattern: "model health|--json|uptime|pid"
---

<objective>
Close the remaining verification gap in the `health` payload/output contract for lifecycle observability.

Purpose: Align runtime and CLI `health` behavior with phase must-haves by adding PID/uptime parity with `status`.
Output: Updated runtime `LifecycleHealth` contract, updated CLI `model health` text/json rendering, and regression tests that enforce the contract.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-CONTEXT.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-RESEARCH.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-VERIFICATION.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-02-SUMMARY.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-03-SUMMARY.md
@src/aurora/runtime/server_lifecycle.py
@src/aurora/cli/model.py
@tests/runtime/test_server_lifecycle.py
@tests/cli/test_model_command.py

<interfaces>
From src/aurora/runtime/server_lifecycle.py:
```python
@dataclass(frozen=True)
class LifecycleHealth:
    ok: bool
    endpoint_url: str
    port: int
    model_id: str
    ownership: ServerOwnership | None
    category: str | None
    message: str
    recovery_commands: tuple[str, ...] = ()
```

From src/aurora/cli/model.py:
```python
def _render_health(health: LifecycleHealth, *, json_output: bool) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runtime health payload contract with PID and uptime metadata</name>
  <files>src/aurora/runtime/server_lifecycle.py, tests/runtime/test_server_lifecycle.py</files>
  <behavior>
    - `LifecycleHealth` carries `pid` and `uptime_seconds` fields in all health responses.
    - `check_health()` maps PID/uptime from `get_status()` for healthy, stopped, and diagnostic error paths.
    - JSON serialization from `LifecycleHealth.to_dict()` includes both fields deterministically.
  </behavior>
  <action>Add `pid` and `uptime_seconds` to `LifecycleHealth` dataclass and `to_dict()`. Update every `LifecycleHealth(...)` construction inside `check_health()` to propagate `status.pid` and `status.uptime_seconds` instead of dropping process metadata. Add runtime tests that assert these fields are present and correctly valued in success and failure health payloads.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_lifecycle.py -q -k "health and (pid or uptime)"</automated>
  </verify>
  <done>Runtime health contract includes PID/uptime across all code paths and is protected by runtime tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose health PID/uptime in CLI text and JSON outputs with regression coverage</name>
  <files>src/aurora/cli/model.py, tests/cli/test_model_command.py</files>
  <behavior>
    - `aurora model health --json` includes `pid` and `uptime_seconds`.
    - Human-readable `aurora model health` output prints PID and uptime lines consistently with status naming.
    - CLI tests validate both JSON payload keys and text rendering for PID/uptime.
  </behavior>
  <action>Update `_render_health` in `model.py` to render PID and uptime for non-JSON output while keeping current message/category/recovery formatting. Ensure JSON mode naturally exposes the new fields via `LifecycleHealth.to_dict()`. Extend CLI tests to assert `model health --json` includes PID/uptime values and plain text output shows `pid` + `uptime(s)` lines.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_model_command.py -q -k "model_health"</automated>
  </verify>
  <done>`model health` text/JSON contract now matches verification expectations for PID/uptime visibility.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_server_lifecycle.py tests/cli/test_model_command.py -q -k "health"` passes.
- `aurora model health --json` includes `pid` and `uptime_seconds` keys.
- `aurora model health` text output displays `pid` and `uptime(s)` lines with stable formatting.
</verification>

<success_criteria>
- Phase 01.1 verification gap for health payload contract is fully closed.
- Runtime/CLI health payload parity with status contract (PID + uptime) is enforced by tests.
- No new lifecycle behavior is introduced beyond the scoped contract fix.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-04-SUMMARY.md`
</output>
