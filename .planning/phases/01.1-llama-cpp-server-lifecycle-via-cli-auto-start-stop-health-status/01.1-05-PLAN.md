---
phase: 01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status
plan: 05
type: execute
wave: 5
depends_on:
  - "01.1-04"
files_modified:
  - tests/cli/test_setup_wizard.py
autonomous: true
gap_closure: true
requirements:
  - CLI-02
  - CLI-04
  - MOD-03
  - PRIV-01
must_haves:
  truths:
    - "Users can complete setup wizard auto-start/bootstrap flows without internal `LifecycleHealth` errors interrupting the experience."
    - "When setup triggers runtime validation retries, Aurora continues toward a ready-to-use runtime instead of aborting mid-flow."
    - "After setup, lifecycle commands (`aurora model start|stop|status|health`) continue to work without regressions from the health contract update."
  artifacts:
    - path: "tests/cli/test_setup_wizard.py"
      provides: "Setup wizard regression fixtures aligned with the current LifecycleHealth contract"
      contains: "LifecycleHealth(...) constructors in retry, auto-start, and bootstrap tests"
    - path: "src/aurora/runtime/server_lifecycle.py"
      provides: "LifecycleHealth dataclass contract required by setup-wizard test fixtures"
      contains: "LifecycleHealth(pid, uptime_seconds, ...)"
  key_links:
    - from: "tests/cli/test_setup_wizard.py"
      to: "src/aurora/runtime/server_lifecycle.py"
      via: "EnsureRuntimeResult.health fixtures instantiate LifecycleHealth with required fields"
      pattern: "LifecycleHealth\\("
    - from: "tests/cli/test_setup_wizard.py"
      to: "src/aurora/cli/setup.py"
      via: "Setup wizard tests exercise ensure_runtime_for_inference auto-start/bootstrap wiring"
      pattern: "ensure_runtime_for_inference|model_bootstrap_callback"
---

<objective>
Close the remaining phase 01.1 verification gap caused by setup-wizard fixtures drifting from the updated `LifecycleHealth(pid, uptime_seconds)` contract.

Purpose: Restore automated proof that setup auto-start/bootstrap behavior works after health payload contract expansion.
Output: Updated setup-wizard test fixtures and a passing broad runtime/CLI regression suite run aligned with verification scope.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-CONTEXT.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-RESEARCH.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-VERIFICATION.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-04-SUMMARY.md
@src/aurora/runtime/server_lifecycle.py
@src/aurora/cli/setup.py
@tests/cli/test_setup_wizard.py

<interfaces>
From src/aurora/runtime/server_lifecycle.py:
```python
@dataclass(frozen=True)
class LifecycleHealth:
    ok: bool
    endpoint_url: str
    port: int
    model_id: str
    ownership: ServerOwnership | None
    pid: int | None
    uptime_seconds: int | None
    category: str | None
    message: str
    recovery_commands: tuple[str, ...] = ()
```

From src/aurora/runtime/server_lifecycle.py:
```python
@dataclass(frozen=True)
class EnsureRuntimeResult:
    settings: RuntimeSettings
    status: LifecycleStatus
    health: LifecycleHealth
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align setup wizard `LifecycleHealth` fixtures with pid/uptime contract</name>
  <files>tests/cli/test_setup_wizard.py</files>
  <behavior>
    - Every setup wizard `LifecycleHealth(...)` fixture includes `pid` and `uptime_seconds`.
    - `health` fixture metadata remains consistent with each paired `LifecycleStatus` fixture in the same test.
    - Previously failing setup wizard tests no longer raise constructor `TypeError`.
  </behavior>
  <action>Update all `EnsureRuntimeResult(..., health=LifecycleHealth(...))` fixtures in `tests/cli/test_setup_wizard.py` to pass `pid` and `uptime_seconds`. Mirror the values from each test's `LifecycleStatus` fixture so `status` and `health` process metadata stay in contract parity.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_setup_wizard.py -q -k "retries_until_runtime_validation_passes or auto_start_path_uses_inference_guard or bootstrap_missing_model_persists_and_continues"</automated>
  </verify>
  <done>Setup wizard regression fixtures are contract-compliant and the three previously failing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run broad regression suite to restore phase verification evidence</name>
  <files>tests/cli/test_setup_wizard.py</files>
  <action>Run the broad runtime/CLI regression suite from `01.1-VERIFICATION.md` after fixture updates. If failures remain and are still within the setup-wizard contract parity scope, fix only those fixture-level issues and re-run until the suite is green.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_state.py tests/runtime/test_server_lock.py tests/runtime/test_server_lifecycle.py tests/cli/test_model_command.py tests/cli/test_setup_wizard.py tests/cli/test_entrypoint.py -q</automated>
  </verify>
  <done>Broad phase regression suite passes and setup wizard auto-start/bootstrap behavior regains automated verification proof.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/cli/test_setup_wizard.py -q -k "retries_until_runtime_validation_passes or auto_start_path_uses_inference_guard or bootstrap_missing_model_persists_and_continues"` passes.
- `uv run pytest tests/runtime/test_server_state.py tests/runtime/test_server_lock.py tests/runtime/test_server_lifecycle.py tests/cli/test_model_command.py tests/cli/test_setup_wizard.py tests/cli/test_entrypoint.py -q` passes.
- No `LifecycleHealth.__init__()` missing `pid`/`uptime_seconds` failures remain.
</verification>

<success_criteria>
- Setup wizard regression fixtures fully match the `LifecycleHealth(pid, uptime_seconds)` contract.
- The remaining verification gap for phase 01.1 auto-start/bootstrap test evidence is closed.
- Phase 01.1 is ready for re-verification with broad regression evidence intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-05-SUMMARY.md`
</output>
