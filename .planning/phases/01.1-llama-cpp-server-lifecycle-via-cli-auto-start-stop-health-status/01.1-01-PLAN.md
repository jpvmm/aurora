---
phase: 01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aurora/runtime/server_state.py
  - src/aurora/runtime/server_lock.py
  - src/aurora/runtime/paths.py
  - tests/runtime/test_server_state.py
  - tests/runtime/test_server_lock.py
autonomous: true
requirements:
  - PRIV-01
  - MOD-03
must_haves:
  truths:
    - "Aurora persists one global lifecycle state per user so multiple terminals see the same server ownership and runtime metadata."
    - "Lifecycle state records enough data for status output: endpoint, port, PID, ownership, model, started_at, and last transition reason."
    - "Concurrent start/stop requests are serialized with a cross-terminal lock to prevent duplicate managed server launches."
  artifacts:
    - path: "src/aurora/runtime/server_state.py"
      provides: "Lifecycle state schema + JSON load/save helpers for global per-user runtime metadata"
      exports: ["ServerLifecycleState", "load_server_state", "save_server_state", "clear_server_state"]
    - path: "src/aurora/runtime/server_lock.py"
      provides: "Cross-terminal lock guard for lifecycle state transitions"
      exports: ["acquire_lifecycle_lock", "LifecycleLockError"]
    - path: "src/aurora/runtime/paths.py"
      provides: "Canonical filesystem paths for lifecycle state and lock files under global config dir"
      exports: ["get_server_state_path", "get_server_lock_path"]
  key_links:
    - from: "src/aurora/runtime/server_state.py"
      to: "src/aurora/runtime/paths.py"
      via: "persisted state path resolution under global user config directory"
      pattern: "get_server_state_path"
    - from: "src/aurora/runtime/server_lock.py"
      to: "src/aurora/runtime/server_state.py"
      via: "atomic lock around state transitions"
      pattern: "acquire_lifecycle_lock"
---

<objective>
Create deterministic global lifecycle state and locking primitives for the managed `llama-server` process.

Purpose: Establish the shared, concurrency-safe runtime contract required for manual commands and auto-start behavior across multiple terminals.
Output: Runtime state + lock modules, path helpers, and focused tests that prove serialization and persistence behavior.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-CONTEXT.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-RESEARCH.md
@src/aurora/runtime/paths.py
@src/aurora/runtime/settings.py

<interfaces>
From src/aurora/runtime/settings.py:
```python
class RuntimeSettings(BaseSettings):
    endpoint_url: str
    model_id: str
```

From src/aurora/runtime/paths.py:
```python
def get_config_dir() -> Path: ...
def ensure_config_dir() -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lifecycle state schema and persistence under global config</name>
  <files>src/aurora/runtime/server_state.py, src/aurora/runtime/paths.py, tests/runtime/test_server_state.py</files>
  <behavior>
    - State payload includes ownership (`managed|external`), pid/process metadata, endpoint URL/port, model id, started_at, and crash/restart counters.
    - State persists in one global per-user location and is loaded consistently from any terminal.
    - Corrupted or stale state payloads fail safely with actionable diagnostics instead of crashing.
  </behavior>
  <action>Create `server_state.py` with a typed dataclass/model and explicit load/save/clear helpers that serialize deterministically to JSON. Extend `paths.py` with dedicated helpers for lifecycle state location under the existing Aurora global config dir. Add tests that validate round-trip serialization, missing-file defaults, and invalid JSON handling with pt-BR actionable messages aligned to existing runtime diagnostics style.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_state.py -q</automated>
  </verify>
  <done>Lifecycle state can be persisted/reloaded safely and deterministically across CLI invocations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-terminal lifecycle lock with timeout and stale-lock recovery</name>
  <files>src/aurora/runtime/server_lock.py, src/aurora/runtime/paths.py, tests/runtime/test_server_lock.py</files>
  <behavior>
    - Start/stop transitions are guarded by an exclusive lock to avoid duplicate managed servers.
    - Lock acquisition supports bounded waiting with explicit timeout failure classification.
    - Stale lock files from dead processes are recoverable without manual filesystem cleanup.
  </behavior>
  <action>Implement `server_lock.py` with context-manager style lock acquisition around lifecycle transitions, using a single lock file in global config. Detect stale ownership (dead PID/process group) and reclaim lock safely. Emit actionable `RuntimeDiagnosticError`/domain errors in pt-BR on lock timeout with exact recovery commands. Cover lock contention and stale lock recovery with deterministic tests.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_lock.py -q</automated>
  </verify>
  <done>Concurrent terminal operations serialize correctly and stale locks no longer block lifecycle operations.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_server_state.py tests/runtime/test_server_lock.py -q` passes.
- Lifecycle metadata and lock files are created under Aurora global config directory and shared across terminals.
- Lock timeout/stale lock paths return actionable pt-BR recovery guidance.
</verification>

<success_criteria>
- Global lifecycle state contract exists and is test-proven for multi-terminal use.
- Locking primitives prevent race conditions before any process orchestration is introduced.
- Foundation is ready for managed/external ownership logic in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-01-SUMMARY.md`
</output>
