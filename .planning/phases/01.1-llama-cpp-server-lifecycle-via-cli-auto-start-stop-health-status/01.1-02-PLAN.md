---
phase: 01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status
plan: 02
type: execute
wave: 2
depends_on:
  - "01.1-01"
files_modified:
  - src/aurora/runtime/server_lifecycle.py
  - src/aurora/runtime/errors.py
  - src/aurora/runtime/__init__.py
  - tests/runtime/test_server_lifecycle.py
autonomous: true
requirements:
  - MOD-03
  - PRIV-01
  - CLI-04
must_haves:
  truths:
    - "Aurora can start `llama-server` in background as a managed process and report whether runtime ownership is `managed` or `external`."
    - "Start flow handles endpoint conflicts by attempting subsequent ports and persists the selected endpoint for cross-terminal continuity."
    - "Unexpected managed-process crashes trigger exactly one automatic restart attempt with explicit user-facing warning when recovery fails."
    - "`status`/`health` runtime payload contracts expose model, endpoint, port, PID, uptime, and readiness in a format the CLI can render as text or JSON."
  artifacts:
    - path: "src/aurora/runtime/server_lifecycle.py"
      provides: "Lifecycle orchestration service for start/stop/status/health and auto-start guard"
      exports: ["ServerLifecycleService", "ensure_runtime_for_inference", "LifecycleStatus"]
    - path: "src/aurora/runtime/errors.py"
      provides: "Extended runtime error categories for lifecycle startup and lock/process failures"
      contains: "actionable pt-BR recovery commands for lifecycle operations"
    - path: "tests/runtime/test_server_lifecycle.py"
      provides: "Behavioral coverage for managed/external ownership, restart policy, and port fallback"
      contains: "subprocess/health probe simulation scenarios"
  key_links:
    - from: "src/aurora/runtime/server_lifecycle.py"
      to: "src/aurora/runtime/server_state.py"
      via: "state transitions for start/stop/crash/restart lifecycle"
      pattern: "load_server_state|save_server_state"
    - from: "src/aurora/runtime/server_lifecycle.py"
      to: "src/aurora/runtime/llama_client.py"
      via: "active readiness checks via /health and /v1/models"
      pattern: "validate_runtime|LlamaRuntimeClient"
    - from: "src/aurora/runtime/server_lifecycle.py"
      to: "src/aurora/runtime/server_lock.py"
      via: "serialized transition guard for multi-terminal operations"
      pattern: "acquire_lifecycle_lock"
---

<objective>
Implement the lifecycle orchestration engine that owns background process management, runtime ownership, and recovery behavior.

Purpose: Deliver deterministic runtime behavior that satisfies manual operations and auto-start constraints without duplicating process logic in CLI commands.
Output: A runtime lifecycle service with typed results and tests covering start/stop/status/health, external reuse decisions, and crash recovery boundaries.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-CONTEXT.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-RESEARCH.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-01-PLAN.md
@src/aurora/runtime/llama_client.py
@src/aurora/runtime/errors.py
@src/aurora/runtime/settings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build managed/external lifecycle orchestration with background process launch</name>
  <files>src/aurora/runtime/server_lifecycle.py, tests/runtime/test_server_lifecycle.py</files>
  <behavior>
    - Start supports managed launch (`subprocess.Popen(..., start_new_session=True)`) and explicit reuse decision when external server is detected.
    - Lifecycle ownership is persisted and drives stop behavior (`stop` kills only managed process unless force semantics are explicit).
    - Server remains active until explicit stop (no idle auto-shutdown).
  </behavior>
  <action>Create `ServerLifecycleService` that coordinates state + lock + health probes to implement `start_server`, `stop_server`, `get_status`, and `check_health`. Include explicit callback hooks for user decisions (reuse external server or launch managed instance), deterministic non-interactive behavior for automation, and process ownership persistence for cross-terminal visibility.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_lifecycle.py -q -k "start or stop or ownership"</automated>
  </verify>
  <done>Lifecycle service can start/stop/report runtime state consistently with managed vs external ownership semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add startup resilience (port fallback + one-time crash restart) and actionable diagnostics</name>
  <files>src/aurora/runtime/server_lifecycle.py, src/aurora/runtime/errors.py, tests/runtime/test_server_lifecycle.py</files>
  <behavior>
    - Port conflicts trigger automatic fallback to subsequent ports with bounded attempts and persisted endpoint update.
    - Unexpected managed-process death triggers one automatic restart attempt and user warning when unsuccessful.
    - Startup failures emit categorized pt-BR diagnostics with exact recovery commands.
  </behavior>
  <action>Implement lifecycle transition handling for startup failure classes (`binary_missing`, `port_conflict_exhausted`, `startup_timeout`, `lock_timeout`, `crash_restart_failed`). Extend runtime error factory/classification where needed so CLI surfaces recovery commands consistently. Add tests for fallback success, fallback exhaustion, single restart attempt limit, and message category coverage.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_lifecycle.py -q -k "fallback or restart or diagnostic"</automated>
  </verify>
  <done>Lifecycle service tolerates common runtime failures with deterministic retry boundaries and actionable guidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provide inference auto-start guard API with model configuration callback contract</name>
  <files>src/aurora/runtime/server_lifecycle.py, src/aurora/runtime/__init__.py, tests/runtime/test_server_lifecycle.py</files>
  <behavior>
    - A single `ensure_runtime_for_inference` entrypoint auto-starts runtime when missing and returns ready settings/runtime metadata.
    - If model is missing at auto-start time, a callback can prompt/persist model once before retrying startup.
    - Guard function is reusable by any inference-dependent CLI command without duplicating lifecycle logic.
  </behavior>
  <action>Add a public inference guard helper to runtime package exports and implement callback contract(s) for model bootstrap + optional confirmation behavior. Ensure this API can run in interactive and non-interactive contexts, failing fast with recovery commands when callbacks are unavailable. Cover model-missing bootstrap and retry flow in tests.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_server_lifecycle.py -q -k "ensure_runtime_for_inference or model_missing"</automated>
  </verify>
  <done>Runtime layer exposes one authoritative auto-start guard that upcoming inference commands can call directly.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_server_lifecycle.py -q` passes.
- Lifecycle service emits typed status/health payloads consumable by CLI text and `--json` rendering.
- Managed crash handling enforces one automatic restart attempt and reports failures with recovery commands.
</verification>

<success_criteria>
- Runtime orchestration exists as a reusable service (CLI-thin/runtime-thick).
- Start/stop/status/health behavior is deterministic across terminals and ownership modes.
- Auto-start/inference guard contract is available for command-level integration in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-02-SUMMARY.md`
</output>
