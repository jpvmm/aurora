---
phase: 01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status
plan: 03
type: execute
wave: 3
depends_on:
  - "01.1-02"
files_modified:
  - src/aurora/cli/model.py
  - src/aurora/cli/setup.py
  - tests/cli/test_model_command.py
  - tests/cli/test_setup_wizard.py
  - tests/cli/test_entrypoint.py
autonomous: true
requirements:
  - CLI-02
  - CLI-04
  - MOD-03
  - PRIV-01
must_haves:
  truths:
    - "User can run `aurora model start`, `aurora model stop`, `aurora model status`, and `aurora model health` with clear pt-BR outputs and optional `--json`."
    - "When Aurora starts a server, CLI output clearly warns that the process remains active and includes the exact stop command."
    - "If configured model changes while server is active, CLI requires confirmation before restart (or explicit non-interactive override flag)."
    - "Inference auto-start path can prompt for model configuration when missing, persist it, then continue startup."
  artifacts:
    - path: "src/aurora/cli/model.py"
      provides: "Expanded model command surface with lifecycle commands, confirmations, and JSON/text rendering"
      contains: "start/stop/status/health command handlers"
    - path: "src/aurora/cli/setup.py"
      provides: "Setup integration with lifecycle auto-start guard for model bootstrap readiness"
      contains: "runtime guard usage in guided flow"
    - path: "tests/cli/test_model_command.py"
      provides: "CLI regression coverage for lifecycle command UX and decision flows"
      contains: "status/health JSON, start/stop guidance, model-change confirmation cases"
  key_links:
    - from: "src/aurora/cli/model.py"
      to: "src/aurora/runtime/server_lifecycle.py"
      via: "CLI command handlers delegate all process/runtime behavior to lifecycle service"
      pattern: "ServerLifecycleService|ensure_runtime_for_inference"
    - from: "src/aurora/cli/model.py"
      to: "src/aurora/runtime/settings.py"
      via: "model persistence and endpoint updates after lifecycle decisions"
      pattern: "load_settings|save_settings"
    - from: "src/aurora/cli/setup.py"
      to: "src/aurora/runtime/server_lifecycle.py"
      via: "auto-start bootstrap path for first-run runtime readiness"
      pattern: "ensure_runtime_for_inference"
---

<objective>
Expose the lifecycle runtime through user-facing CLI commands and integrate auto-start/bootstrap behavior into existing command flows.

Purpose: Complete the phase by delivering operational commands and user-safe interaction patterns required by locked decisions.
Output: Updated `model` and setup command behavior with comprehensive CLI tests for start/stop/status/health, confirmations, and JSON output.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-CONTEXT.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-RESEARCH.md
@.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-02-PLAN.md
@src/aurora/cli/model.py
@src/aurora/cli/setup.py
@tests/cli/test_model_command.py

<interfaces>
From src/aurora/cli/model.py:
```python
@model_app.command("set")
def model_set_command(...): ...
```

From src/aurora/runtime/settings.py:
```python
def load_settings() -> RuntimeSettings: ...
def save_settings(settings: RuntimeSettings) -> RuntimeSettings: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `model start|stop|status|health` with text + JSON contracts</name>
  <files>src/aurora/cli/model.py, tests/cli/test_model_command.py, tests/cli/test_entrypoint.py</files>
  <behavior>
    - `aurora model start` launches/reuses runtime according to lifecycle ownership decisions and prints post-start "server remains active" guidance.
    - `aurora model stop` stops managed server safely and clearly reports behavior when server is external.
    - `aurora model status` and `aurora model health` support human-readable output and deterministic `--json`.
  </behavior>
  <action>Extend `model_app` with new lifecycle commands and CLI flags required for deterministic behavior (`--json`, `--yes`, `--force` where needed). Keep command handlers thin by delegating lifecycle logic to runtime service. Ensure `status` output includes model, endpoint, port, pid/process ownership, uptime, and lifecycle state; ensure `health` performs active endpoint + model checks and returns actionable diagnostics on failure.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_model_command.py tests/cli/test_entrypoint.py -q -k "start or stop or status or health"</automated>
  </verify>
  <done>Model command group exposes complete lifecycle operational surface with stable text/JSON outputs and passing CLI coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce safe model-change and external-reuse confirmations</name>
  <files>src/aurora/cli/model.py, tests/cli/test_model_command.py</files>
  <behavior>
    - Changing configured model while server is active requires explicit confirmation before restart.
    - Reusing existing external runtime requires explicit user confirmation in interactive mode.
    - Non-interactive contexts fail fast unless explicit override flags are provided.
  </behavior>
  <action>Update `model_set_command` and lifecycle command interaction points so active-runtime model changes prompt for restart confirmation and respect automation flags. Ensure reuse decisions for external runtime are surfaced clearly and never terminate third-party processes unexpectedly. Keep all user messages in practical pt-BR with exact recovery commands when operation is blocked.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_model_command.py -q -k "confirmation or external or restart"</automated>
  </verify>
  <done>Model updates and external runtime reuse paths are safe, explicit, and deterministic across interactive/non-interactive usage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire inference auto-start/bootstrap flow into existing command path</name>
  <files>src/aurora/cli/setup.py, src/aurora/cli/model.py, tests/cli/test_setup_wizard.py, tests/cli/test_model_command.py</files>
  <behavior>
    - Auto-start helper is invoked in an existing inference-dependent flow so runtime startup is no longer purely manual.
    - Missing model at auto-start triggers prompt/persist workflow, then retries startup.
    - Start failures surface categorized pt-BR diagnostics with exact next commands.
  </behavior>
  <action>Integrate `ensure_runtime_for_inference` into the setup/runtime readiness flow (and any current inference-capable path available in this codebase) so startup is automatic when needed. When no model is configured, prompt once, persist through existing settings/model flow, and continue. Add CLI tests validating missing-model bootstrap, startup failure diagnostics, and successful continuation path.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_setup_wizard.py tests/cli/test_model_command.py -q -k "auto-start or bootstrap or missing model"</automated>
  </verify>
  <done>Aurora can auto-start runtime in supported inference paths with guided model bootstrap and actionable failure recovery.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/cli/test_model_command.py tests/cli/test_setup_wizard.py tests/cli/test_entrypoint.py -q` passes.
- All lifecycle command outputs are available in text and `--json` modes.
- Confirmations/overrides behave deterministically in interactive and CI-style non-interactive usage.
</verification>

<success_criteria>
- Phase 01.1 lifecycle command surface is fully accessible from `aurora model`.
- Users can control and inspect server lifecycle without manual process management.
- Auto-start + model bootstrap flow minimizes friction while preserving local-only guardrails and actionable diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status/01.1-03-SUMMARY.md`
</output>
