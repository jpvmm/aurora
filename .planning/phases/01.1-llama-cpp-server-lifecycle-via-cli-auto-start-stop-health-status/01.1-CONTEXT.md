# Phase 01.1: llama.cpp server lifecycle via CLI (auto start/stop + health/status) - Context

**Gathered:** 2026-03-02
**Status:** Ready for planning

<domain>
## Phase Boundary

Implementar ciclo de vida de servidor `llama.cpp` no CLI do Aurora de forma simples para o usuário final: start automático sob demanda, start/stop manual quando necessário e comandos dedicados de status/health. Esta fase define o comportamento operacional do runtime de modelo, sem expandir escopo para ingestão de vault, retrieval de conhecimento ou memória de longo prazo.

</domain>

<decisions>
## Implementation Decisions

### Server lifecycle
- Start principal deve ser **auto-start sob demanda** (quando um comando depende de inferência).
- Também deve existir comando manual explícito: **`aurora model start`**.
- Deve existir comando manual explícito de parada: **`aurora model stop`**.
- Escopo do servidor gerenciado é **global por usuário** (compartilhado entre terminais).
- Política de desligamento automático: **não desligar sozinho** (somente manual).
- Aurora deve exibir aviso claro quando o servidor permanecer ativo e como pará-lo.
- Se o usuário trocar de modelo com servidor ativo, Aurora deve **pedir confirmação** antes de reiniciar.
- Se já existir servidor externo `llama.cpp`, Aurora deve **perguntar ao usuário** se reutiliza ou não.
- Servidor iniciado pelo Aurora deve rodar em **background**.
- Em queda inesperada, Aurora tenta **auto-restart 1x** e avisa o usuário.
- Em conflito de porta, Aurora tenta **próxima porta automaticamente**.
- Se auto-start disparar sem modelo configurado, Aurora deve **pedir modelo e persistir**.
- O trigger de auto-start cobre **qualquer comando que dependa de inferência**.

### Model startup UX
- Usuário deve passar o modelo uma vez e Aurora deve persistir para uso futuro.
- Fluxo deve manter fricção mínima: informar modelo e usar, sem exigir gerenciamento manual constante de processo.

### Health/status contract
- Comandos oficiais de observabilidade da fase: **`aurora model status`** e **`aurora model health`**.
- `model status` deve mostrar resumo operacional: modelo ativo, endpoint, porta, PID/processo e uptime.
- `model health` deve fazer checagem ativa de endpoint + disponibilidade do modelo.
- Saída padrão deve ser legível para humanos, com opção estruturada via **`--json`**.

### Failure/recovery behavior
- Falhas de start devem retornar diagnóstico claro + comando exato de recuperação.
- Mensagens devem manter padrão prático e acionável em pt-BR.

### Claude's Discretion
- Definir conjunto exato de flags opcionais (`--json`, timeout, force, etc.) por comando.
- Definir formato textual final das mensagens de sucesso/erro/aviso mantendo consistência com o padrão já existente no CLI.
- Definir detalhes de priorização quando houver múltiplos endpoints candidatos disponíveis.

</decisions>

<specifics>
## Specific Ideas

- "aurora must be able to serve llama.cpp models through its cli commands and in a very easy way to the public."
- "it must handle the standup of the server and disable it automatically based on user needs."
- "the only thing that the user must do is to pass the model that aurora must start with."
- "aurora must have commands to check the model health and status."

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/aurora/cli/model.py`: fluxo atual de `model set`, integração com settings e guardrails de local-only.
- `src/aurora/cli/setup.py`: wizard de setup e fluxo de validação guiada.
- `src/aurora/cli/doctor.py`: diagnóstico e output acionável de runtime.
- `src/aurora/runtime/llama_client.py`: validação ativa de runtime (`/health` e `/v1/models`).
- `src/aurora/runtime/settings.py`: persistência global por usuário.
- `src/aurora/privacy/policy.py`: validações de política local-only.

### Established Patterns
- Erros em pt-BR com comandos de recuperação explícitos.
- Configuração global por usuário (não por diretório).
- Validação de política no boundary de settings.

### Integration Points
- Estender `model` command group para incluir `start`, `stop`, `status`, `health`.
- Reutilizar `llama_client` para health ativo e verificação de modelo.
- Reutilizar settings globais para endpoint/modelo e estado operacional persistido quando necessário.
- Preservar compatibilidade de saída com `doctor` e `config show`.

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---
*Phase: 01.1-llama-cpp-server-lifecycle-via-cli-auto-start-stop-health-status*
*Context gathered: 2026-03-02*
