---
phase: 04-long-term-memory-fusion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/aurora/runtime/paths.py
  - src/aurora/runtime/settings.py
  - src/aurora/retrieval/contracts.py
  - src/aurora/memory/__init__.py
  - src/aurora/memory/store.py
  - tests/memory/__init__.py
  - tests/memory/test_store.py
autonomous: true
requirements:
  - MEM-01
  - MEM-04

must_haves:
  truths:
    - "get_memory_dir() returns ~/Library/Application Support/aurora/memory/ (or AURORA_CONFIG_DIR override)"
    - "get_preferences_path() returns ~/Library/Application Support/aurora/preferences.md"
    - "EpisodicMemoryStore.write() creates timestamped markdown files with YAML frontmatter"
    - "EpisodicMemoryStore.list_memories() returns parsed frontmatter from all episodic files"
    - "EpisodicMemoryStore.clear() deletes all .md files from the memory directory"
    - "RetrievedNote has a source field distinguishing vault from memory hits"
    - "RuntimeSettings has memory_top_k and memory_min_score fields"
  artifacts:
    - path: "src/aurora/runtime/paths.py"
      provides: "get_memory_dir, get_preferences_path path helpers"
      contains: "MEMORY_DIRNAME"
    - path: "src/aurora/memory/store.py"
      provides: "EpisodicMemoryStore class"
      exports: ["EpisodicMemoryStore", "MEMORY_COLLECTION"]
    - path: "src/aurora/retrieval/contracts.py"
      provides: "source field on RetrievedNote"
      contains: 'source: str = "vault"'
    - path: "tests/memory/test_store.py"
      provides: "Unit tests for EpisodicMemoryStore"
      min_lines: 60
  key_links:
    - from: "src/aurora/memory/store.py"
      to: "src/aurora/runtime/paths.py"
      via: "get_memory_dir() import"
      pattern: "from aurora\\.runtime\\.paths import get_memory_dir"
---

<objective>
Build the memory foundation layer: path helpers, extended contracts with source tracking, episodic memory file store, settings extension, and PyYAML direct dependency.

Purpose: All subsequent memory plans depend on these primitives. No memory files can be written, listed, or cleared without EpisodicMemoryStore. No dual-source retrieval without the source field on RetrievedNote.
Output: src/aurora/memory/ module with store.py, extended paths.py/contracts.py/settings.py, and unit tests.
</objective>

<execution_context>
@/Users/jp/Projects/Personal/aurora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jp/Projects/Personal/aurora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-long-term-memory-fusion/04-CONTEXT.md
@.planning/phases/04-long-term-memory-fusion/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/aurora/runtime/paths.py:
```python
APP_NAME = "aurora"
CONFIG_DIR_ENV = "AURORA_CONFIG_DIR"

def get_config_dir() -> Path: ...
def ensure_config_dir() -> Path: ...
```

From src/aurora/retrieval/contracts.py:
```python
@dataclass(frozen=True)
class RetrievedNote:
    path: str
    score: float
    content: str

@dataclass(frozen=True)
class QMDSearchHit:
    path: str
    score: float
    title: str
    snippet: str
```

From src/aurora/runtime/settings.py:
```python
class RuntimeSettings(BaseSettings):
    retrieval_top_k: int = 7
    retrieval_min_score: float = 0.30
    chat_history_max_turns: int = 10
    # ... existing fields
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PyYAML dependency, path helpers, settings fields, and source field on contracts</name>
  <files>pyproject.toml, src/aurora/runtime/paths.py, src/aurora/runtime/settings.py, src/aurora/retrieval/contracts.py</files>
  <read_first>
    - pyproject.toml
    - src/aurora/runtime/paths.py
    - src/aurora/runtime/settings.py
    - src/aurora/retrieval/contracts.py
    - tests/runtime/test_paths.py (if exists)
    - tests/retrieval/test_retrieval_service.py
  </read_first>
  <behavior>
    - Test: get_memory_dir() returns get_config_dir() / "memory"
    - Test: get_preferences_path() returns get_config_dir() / "preferences.md"
    - Test: RetrievedNote(path="x", score=0.9, content="c") has source="vault" by default
    - Test: RetrievedNote(path="x", score=0.9, content="c", source="memory") has source="memory"
    - Test: RuntimeSettings() has memory_top_k=5 and memory_min_score=0.25
    - Test: RuntimeSettings(memory_top_k=3) raises ValidationError (below 3 minimum)
    - Test: RuntimeSettings(memory_top_k=15) raises ValidationError (above 10 maximum)
  </behavior>
  <action>
    1. Run `uv add pyyaml` to add PyYAML as a direct dependency in pyproject.toml.

    2. In `src/aurora/runtime/paths.py`, add two constants and two functions following the existing pattern:
       ```python
       MEMORY_DIRNAME = "memory"
       PREFERENCES_FILENAME = "preferences.md"

       def get_memory_dir() -> Path:
           """Return the directory for Aurora-managed episodic memory files."""
           return get_config_dir() / MEMORY_DIRNAME

       def get_preferences_path() -> Path:
           """Return the path for the Tier 1 preferences markdown file."""
           return get_config_dir() / PREFERENCES_FILENAME
       ```

    3. In `src/aurora/retrieval/contracts.py`, add `source: str = "vault"` field to `RetrievedNote` (per D-16: vault vs memory distinction). Keep the frozen dataclass pattern. Default is "vault" for backward compatibility so existing retrieval code is unaffected. Add to `__all__`.

    4. In `src/aurora/runtime/settings.py`, add two new fields to `RuntimeSettings`:
       ```python
       memory_top_k: int = 5
       memory_min_score: float = 0.25
       ```
       Add a field_validator for memory_top_k enforcing range 3-10:
       ```python
       @field_validator("memory_top_k")
       @classmethod
       def _validate_memory_top_k(cls, value: int) -> int:
           if value < 3 or value > 10:
               raise ValueError("memory_top_k deve estar entre 3 e 10.")
           return value
       ```

    Per D-03: memory files stored in ~/Library/Application Support/aurora/memory/.
    Per D-04: preferences at ~/Library/Application Support/aurora/preferences.md.
    Per D-16: source field enables citation format switching (vault vs memory).
  </action>
  <verify>
    <automated>uv run pytest tests/runtime/ tests/retrieval/ -x -q</automated>
  </verify>
  <done>
    - `uv add pyyaml` succeeded and pyproject.toml lists pyyaml
    - get_memory_dir() and get_preferences_path() exist and return correct paths
    - RetrievedNote has source field defaulting to "vault"
    - RuntimeSettings has memory_top_k (default 5, range 3-10) and memory_min_score (default 0.25)
    - All existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EpisodicMemoryStore with write, list, and clear operations</name>
  <files>src/aurora/memory/__init__.py, src/aurora/memory/store.py, tests/memory/__init__.py, tests/memory/test_store.py</files>
  <read_first>
    - src/aurora/runtime/paths.py (after Task 1 modifications)
    - src/aurora/kb/qmd_backend.py (for QMD collection pattern reference)
  </read_first>
  <behavior>
    - Test: write() creates a .md file in the memory directory with YAML frontmatter containing date, topic, turn_count, source fields
    - Test: write() file body matches the provided summary text
    - Test: write() filename follows YYYY-MM-DDTHH-MM.md format (per D-06)
    - Test: write() with collision (same minute) appends -2, -3 suffix (per Pitfall 7)
    - Test: list_memories() returns list of dicts with date, topic, turn_count, source, filename keys
    - Test: list_memories() returns empty list when directory doesn't exist
    - Test: list_memories() returns sorted results (chronological by filename)
    - Test: clear() deletes all .md files and returns count deleted
    - Test: clear() returns 0 when directory doesn't exist
    - Test: write() creates directory if it doesn't exist (parents=True)
  </behavior>
  <action>
    1. Create `src/aurora/memory/__init__.py` as empty module init.

    2. Create `src/aurora/memory/store.py` with `EpisodicMemoryStore` class:
       - Constructor accepts optional `memory_dir: Path | None`, defaults to `get_memory_dir()`
       - `MEMORY_COLLECTION = "aurora-memory"` module constant (per D-05)
       - `write(*, topic: str, turn_count: int, summary: str) -> Path`:
         - Creates directory with `parents=True, exist_ok=True`
         - Generates filename: `datetime.now(UTC).strftime("%Y-%m-%dT%H-%M") + ".md"` (per D-06)
         - Collision detection: if path exists, try `-2.md`, `-3.md`, etc. (per Pitfall 7)
         - YAML frontmatter with `date` (ISO date string), `topic`, `turn_count`, `source: "chat"` (per D-07)
         - Use `yaml.dump()` with `allow_unicode=True, default_flow_style=False`
         - Write file: `---\n{frontmatter}\n---\n\n{summary}\n`
         - Returns the written Path
       - `list_memories() -> list[dict]`:
         - Returns `[]` if directory doesn't exist
         - Glob `*.md`, sort by filename (natural chronological order)
         - For each file, parse frontmatter between `---` delimiters using `yaml.safe_load`
         - Return list of dicts with frontmatter fields + `filename` key
       - `clear() -> int`:
         - Returns 0 if directory doesn't exist
         - Delete all `*.md` files via `f.unlink()`
         - Return count deleted
       - Private `_parse_frontmatter(path: Path) -> dict` helper:
         - Read file text, split on `---` markers, `yaml.safe_load` the frontmatter block
         - Return empty dict on parse failure (defensive)

    3. Create `tests/memory/__init__.py` as empty init.

    4. Create `tests/memory/test_store.py` with tests covering all behaviors above. Use `tmp_path` fixture to inject a temporary memory directory. Import `EpisodicMemoryStore` and assert file contents, frontmatter parsing, collision handling, and clear semantics.

    Per D-03: files in ~/Library/Application Support/aurora/memory/.
    Per D-06: ISO timestamp filenames.
    Per D-07: YAML frontmatter with date/topic/turn_count/source.
    Per Pitfall 7: collision suffix.
  </action>
  <verify>
    <automated>uv run pytest tests/memory/test_store.py -x -v</automated>
  </verify>
  <done>
    - src/aurora/memory/store.py exists with EpisodicMemoryStore class
    - write() creates correctly formatted episodic files with YAML frontmatter
    - Filename collision handling appends -2, -3 suffixes
    - list_memories() parses frontmatter from all .md files
    - clear() removes all .md files and returns count
    - All tests in tests/memory/test_store.py pass
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/memory/ tests/runtime/ tests/retrieval/ -x -q
</verification>

<success_criteria>
- PyYAML is a direct dependency in pyproject.toml
- get_memory_dir() and get_preferences_path() return correct platform paths
- RetrievedNote has source field (default "vault", supports "memory")
- RuntimeSettings has memory_top_k and memory_min_score with validation
- EpisodicMemoryStore write/list/clear operations work correctly with unit tests
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-long-term-memory-fusion/04-01-SUMMARY.md`
</output>
