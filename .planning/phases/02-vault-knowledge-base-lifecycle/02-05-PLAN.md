---
phase: 02-vault-knowledge-base-lifecycle
plan: 05
type: execute
wave: 4
depends_on:
  - "02-04"
files_modified:
  - src/aurora/kb/contracts.py
  - src/aurora/kb/qmd_adapter.py
  - src/aurora/kb/qmd_backend.py
  - src/aurora/kb/service.py
  - src/aurora/runtime/paths.py
  - src/aurora/runtime/settings.py
  - tests/kb/test_qmd_adapter.py
  - tests/kb/test_qmd_backend.py
  - tests/runtime/test_kb_service.py
autonomous: true
gap_closure: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04
  - KB-05
  - PRIV-02
must_haves:
  truths:
    - "KB ingest/update/delete/rebuild mutate a real QMD-backed index path, not only Aurora manifest state."
    - "Index mutations stay deterministic and scoped: only in-scope markdown notes are added/updated/removed."
    - "Backend failures surface typed privacy-safe diagnostics with recovery guidance and never leak note content."
  artifacts:
    - path: "src/aurora/kb/qmd_backend.py"
      provides: "Concrete QMD CLI transport implementing QMDBackend protocol"
      contains: "command execution, index/collection bootstrap, typed error mapping"
    - path: "src/aurora/kb/service.py"
      provides: "Lifecycle orchestration wired to concrete backend and prepared-content pipeline"
      contains: "backend selection, apply/remove/rebuild flow with manifest commit ordering"
    - path: "tests/kb/test_qmd_backend.py"
      provides: "Unit coverage for QMD command mapping and failure handling"
      contains: "bootstrap/update/remove/rebuild command expectations and diagnostic assertions"
  key_links:
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/kb/qmd_backend.py"
      via: "service backend factory and operation delegation"
      pattern: "QMD.*Backend|run_ingest|run_update|run_delete|run_rebuild"
    - from: "src/aurora/kb/qmd_adapter.py"
      to: "src/aurora/kb/qmd_backend.py"
      via: "adapter apply/remove/rebuild contract"
      pattern: "apply|remove|rebuild"
    - from: "src/aurora/kb/qmd_adapter.py"
      to: "src/aurora/kb/manifest.py"
      via: "manifest commit only after successful backend mutation"
      pattern: "_commit_manifest|state_mutated"
---

<objective>
Close the "real backend integration" verification gap by replacing the Phase 2 placeholder backend path with a concrete QMD-backed transport.

Purpose: Ensure KB lifecycle commands are proven against a real index mutation path while preserving deterministic Aurora semantics.
Output: Concrete backend implementation, service wiring, and regression coverage for QMD-backed lifecycle operations.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-CONTEXT.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-RESEARCH.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-VERIFICATION.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-04-SUMMARY.md
@src/aurora/kb/service.py
@src/aurora/kb/qmd_adapter.py
@src/aurora/runtime/paths.py
@src/aurora/runtime/settings.py
@tests/kb/test_qmd_adapter.py
@tests/runtime/test_kb_service.py

<interfaces>
From src/aurora/kb/qmd_adapter.py:
```python
class QMDBackend(Protocol):
    def apply(self, paths: tuple[str, ...]) -> QMDBackendResponse: ...
    def remove(self, paths: tuple[str, ...]) -> QMDBackendResponse: ...
    def rebuild(self, paths: tuple[str, ...]) -> QMDBackendResponse: ...
```

From src/aurora/kb/service.py:
```python
class KBService:
    def run_ingest(...): ...
    def run_update(...): ...
    def run_delete(...): ...
    def run_rebuild(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement concrete QMD backend transport and deterministic backend settings</name>
  <files>src/aurora/kb/qmd_backend.py, src/aurora/runtime/paths.py, src/aurora/runtime/settings.py</files>
  <behavior>
    - Backend executes real `qmd` commands through subprocess with explicit argv (no shell string interpolation).
    - Backend uses Aurora-managed index/collection names so tests and user runs are isolated and deterministic.
    - Command failures are mapped into `QMDBackendDiagnostic` with stable categories and recovery hints.
  </behavior>
  <action>Create `QMDCliBackend` implementing `QMDBackend` and encapsulating command execution (`qmd --index &lt;name&gt; ...`). Add runtime settings/paths needed to keep backend state deterministic (index name + collection name + managed corpus directory under Aurora config). Include bootstrap logic that creates/updates the managed collection before operations, and typed mapping for command stderr/exit failures without exposing markdown content.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_qmd_backend.py -q</automated>
  </verify>
  <done>Real QMD command transport exists, is deterministic, and emits typed diagnostics on backend failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire service and adapter to prepared-content backend flow without breaking manifest guarantees</name>
  <files>src/aurora/kb/contracts.py, src/aurora/kb/qmd_adapter.py, src/aurora/kb/service.py, tests/runtime/test_kb_service.py</files>
  <behavior>
    - Ingest/update/rebuild pass prepared in-scope markdown payloads to backend apply path.
    - Delete removes indexed notes from backend-managed corpus/index only (never touches user vault files).
    - Manifest state mutates only after backend success for apply/remove/rebuild operations.
  </behavior>
  <action>Extend KB internal contracts so service can pass prepared markdown payloads (relative path + cleaned text metadata) into adapter/backend operations. Replace default `_NoopQMDBackend` usage with `QMDCliBackend` factory while preserving dependency injection for tests. Keep existing scope/delta rules and fail-fast divergence handling unchanged; only backend transport path changes. Ensure unreadable note handling remains diagnostic-only and does not trigger unintended removals.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_kb_service.py tests/kb/test_qmd_adapter.py -q</automated>
  </verify>
  <done>KB lifecycle commands are backed by concrete QMD transport with intact deterministic state and error semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for QMD backend command sequencing and failure mapping</name>
  <files>tests/kb/test_qmd_backend.py, tests/kb/test_qmd_adapter.py, tests/runtime/test_kb_service.py</files>
  <behavior>
    - Tests prove bootstrap + update/rebuild/remove command sequences are correct for each lifecycle operation.
    - Tests prove backend command failures map to typed diagnostics with recovery commands.
    - Tests prove no raw note body appears in backend-related error output.
  </behavior>
  <action>Create/expand backend-focused tests using subprocess stubs to assert exact command argv and mutation ordering for ingest/update/delete/rebuild. Add failure-path assertions for non-zero exits, missing `qmd` binary, and partial backend diagnostics propagation. Preserve test isolation by using temporary config dirs and backend identifiers unique per test run.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_qmd_backend.py tests/kb/test_qmd_adapter.py tests/runtime/test_kb_service.py -q -k "qmd or backend or kb_service"</automated>
  </verify>
  <done>Backend path is covered by deterministic automated tests and no longer depends on fake-only verification.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kb/test_qmd_backend.py -q` passes.
- `uv run pytest tests/runtime/test_kb_service.py tests/kb/test_qmd_adapter.py -q` passes.
- `uv run pytest tests/kb/test_qmd_backend.py tests/kb/test_qmd_adapter.py tests/runtime/test_kb_service.py -q -k "qmd or backend or kb_service"` passes.
</verification>

<success_criteria>
- Phase 2 lifecycle commands mutate a concrete QMD-backed backend path instead of a no-op backend.
- Manifest/index state ordering remains deterministic and recoverable after failures.
- Backend command failures are surfaced as privacy-safe actionable diagnostics through CLI contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-knowledge-base-lifecycle/02-05-SUMMARY.md`
</output>
