---
phase: 02-vault-knowledge-base-lifecycle
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/aurora/cli/app.py
  - src/aurora/cli/kb.py
  - src/aurora/kb/service.py
  - tests/cli/test_entrypoint.py
  - tests/cli/test_kb_command.py
  - tests/runtime/test_kb_service.py
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04
  - KB-05
  - PRIV-02
must_haves:
  truths:
    - "User can run `aurora kb ingest <vault_path>` to index scoped markdown notes with readable progress and final stats."
    - "User can run `aurora kb update` to process only changed notes and automatically remove notes deleted from vault."
    - "User can run `aurora kb delete` (index-only) and `aurora kb rebuild` safely, with actionable fail-fast guidance on critical errors."
    - "Text and `--json` outputs expose the same counters (`read/indexed/updated/removed/skipped/errors`), duration, and effective scope."
  artifacts:
    - path: "src/aurora/kb/service.py"
      provides: "KB lifecycle orchestration across scan/scope/preprocess/delta/adapter/manifest"
      contains: "ingest/update/delete/rebuild service operations and operation summary builder"
    - path: "src/aurora/cli/kb.py"
      provides: "User-facing `aurora kb` commands with text/JSON rendering and fail-fast diagnostics"
      contains: "ingest/update/delete/rebuild command handlers and progress rendering"
    - path: "tests/cli/test_kb_command.py"
      provides: "CLI regression coverage for lifecycle command semantics and output contracts"
      contains: "ingest path requirement, update incremental behavior, delete semantics, rebuild stats"
  key_links:
    - from: "src/aurora/cli/kb.py"
      to: "src/aurora/kb/service.py"
      via: "Command handlers delegate lifecycle operations and render shared summaries"
      pattern: "KBService\\.|run_ingest|run_update|run_delete|run_rebuild"
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/kb/scanner.py"
      via: "Service enforces scoped markdown discovery before any index mutation"
      pattern: "scan|scope|dry_run"
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/kb/manifest.py"
      via: "Manifest commits only after successful adapter operations"
      pattern: "load_manifest|save_manifest"
---

<objective>
Deliver complete Phase 2 user-facing KB lifecycle commands by wiring CLI to service orchestration and final verification coverage.

Purpose: Turn Wave 1/2 building blocks into executable ingest/update/delete/rebuild behavior that satisfies all phase requirements.
Output: Production KB service and CLI command implementations with integration-level tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-CONTEXT.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-RESEARCH.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-01-PLAN.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-02-PLAN.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-03-PLAN.md
@src/aurora/cli/kb.py
@src/aurora/kb/contracts.py
@src/aurora/kb/scope.py
@src/aurora/kb/scanner.py
@src/aurora/kb/preprocess.py
@src/aurora/kb/delta.py
@src/aurora/kb/manifest.py
@src/aurora/kb/qmd_adapter.py

<interfaces>
From src/aurora/cli/kb.py:
```python
@kb_app.command("ingest")
def kb_ingest_command(vault_path: str, ...): ...
```

From src/aurora/kb/contracts.py:
```python
class KBOperationSummary(...):
    counters: KBOperationCounters
    duration_seconds: float
    effective_scope: list[str]
```

From src/aurora/kb/delta.py:
```python
def classify_delta(..., verify_hash: bool = False) -> KBDeltaResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement KB service orchestration for ingest/update/delete/rebuild</name>
  <files>src/aurora/kb/service.py, tests/runtime/test_kb_service.py</files>
  <behavior>
    - Ingest runs scan -> scope filter -> preprocess -> adapter apply -> manifest commit.
    - Update runs scope-aware delta classification and applies only changed/removed sets.
    - Rebuild clears prior index state safely and re-ingests with same preprocessing and scope rules.
  </behavior>
  <action>Create `KBService` in `src/aurora/kb/service.py` that orchestrates modules from Wave 2 and builds a single immutable operation summary object. Enforce fail-fast behavior for critical state divergence/corruption with `aurora kb rebuild` guidance, while allowing per-file non-critical errors to accumulate in summary counters. Ensure update respects configured scope and auto-removes vanished files from index/manifest.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_kb_service.py -q</automated>
  </verify>
  <done>Service operations satisfy ingest/update/delete/rebuild semantics with deterministic counters and error handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CLI command handlers to service with stable text/JSON output contracts</name>
  <files>src/aurora/cli/kb.py, src/aurora/cli/app.py, tests/cli/test_kb_command.py, tests/cli/test_entrypoint.py</files>
  <behavior>
    - `aurora kb ingest &lt;vault_path&gt;` requires explicit path and supports `--dry-run` preview.
    - `aurora kb update`, `aurora kb delete`, and `aurora kb rebuild` render progress and final stats in text or `--json`.
    - CLI errors include path + category + recovery hint without note content leakage.
  </behavior>
  <action>Replace skeleton handlers in `src/aurora/cli/kb.py` with service-backed implementations, preserving pt-BR operational guidance and deterministic JSON output. Ensure `delete` is index-only (never deletes vault files), include `--verify-hash` for update precision, and keep long-running commands progressive (stage + counters) with final summary totals and effective scope. Update root help tests if needed after command options settle.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kb_command.py tests/cli/test_entrypoint.py -q -k "kb"</automated>
  </verify>
  <done>CLI command surface is fully operational and scriptable with aligned text/JSON behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add end-to-end regression coverage for lifecycle semantics and privacy-safe logs</name>
  <files>tests/runtime/test_kb_service.py, tests/cli/test_kb_command.py</files>
  <behavior>
    - Test suite proves incremental updates skip unchanged notes and remove deleted notes.
    - Test suite proves rebuild reprocesses full effective scope and resets stale state.
    - Test suite proves logs/counters expose operational stats without printing note body content.
  </behavior>
  <action>Expand runtime + CLI tests with controlled vault fixtures covering ingest, update (changed/unchanged/removed), explicit delete, rebuild, dry-run scope preview, zero-match include failure, and divergence error requiring rebuild. Assert final summary counters and verify that diagnostics include path/category/hint while excluding raw markdown content.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_kb_service.py tests/cli/test_kb_command.py -q</automated>
  </verify>
  <done>Phase 2 behavior is proven by automated tests aligned to all KB and PRIV requirements.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_kb_service.py -q` passes.
- `uv run pytest tests/cli/test_kb_command.py tests/cli/test_entrypoint.py -q -k "kb"` passes.
- `uv run pytest tests/runtime/test_kb_service.py tests/cli/test_kb_command.py -q` passes.
</verification>

<success_criteria>
- All Phase 2 requirements (KB-01..KB-05, PRIV-02) are executable through `aurora kb` commands.
- Incremental, delete, and rebuild operations are deterministic and recoverable.
- Progress and final stats are readable, privacy-safe, and contract-consistent across text/JSON outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-knowledge-base-lifecycle/02-04-SUMMARY.md`
</output>
