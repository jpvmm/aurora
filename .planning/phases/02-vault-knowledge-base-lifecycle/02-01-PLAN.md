---
phase: 02-vault-knowledge-base-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aurora/cli/app.py
  - src/aurora/cli/kb.py
  - src/aurora/kb/__init__.py
  - src/aurora/kb/contracts.py
  - src/aurora/runtime/paths.py
  - src/aurora/runtime/settings.py
  - tests/cli/test_entrypoint.py
  - tests/kb/test_contracts.py
  - tests/runtime/test_settings_defaults.py
autonomous: true
requirements:
  - KB-01
  - KB-05
  - PRIV-02
must_haves:
  truths:
    - "User can discover a dedicated `aurora kb` namespace with lifecycle commands aligned to vault operations."
    - "KB vault path and include/exclude scope defaults persist globally with deterministic serialization."
    - "KB command responses share one operation-summary contract that can power text and `--json` outputs without divergence."
  artifacts:
    - path: "src/aurora/cli/kb.py"
      provides: "Typer `kb` command group entrypoint for ingest/update/delete/rebuild command surface"
      contains: "kb_app = typer.Typer(...) and command definitions"
    - path: "src/aurora/kb/contracts.py"
      provides: "Typed KB operation contracts for counters, summaries, scope rules, and diagnostics"
      contains: "dataclasses/pydantic models for operation summary and scope configuration"
    - path: "src/aurora/runtime/settings.py"
      provides: "Global KB settings persistence inside existing runtime settings model"
      contains: "vault path + include/exclude scope fields with defaults"
  key_links:
    - from: "src/aurora/cli/app.py"
      to: "src/aurora/cli/kb.py"
      via: "Root CLI registers kb namespace"
      pattern: "app\\.add_typer\\(kb_app, name=\"kb\"\\)"
    - from: "src/aurora/cli/kb.py"
      to: "src/aurora/kb/contracts.py"
      via: "CLI serialization reads one canonical summary contract for text/json rendering"
      pattern: "KBOperationSummary|KBScopeConfig"
    - from: "src/aurora/runtime/settings.py"
      to: "src/aurora/kb/contracts.py"
      via: "Persisted scope configuration maps directly to KB scope model"
      pattern: "include|exclude|vault"
---

<objective>
Create the Phase 2 foundation: KB command namespace, global scope persistence, and shared lifecycle data contracts.

Purpose: Lock command and state contracts before implementation so Wave 2 plans can execute in parallel with no interface drift.
Output: New `aurora kb` CLI group, KB contracts module, and runtime settings/path extensions with regression tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-CONTEXT.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-RESEARCH.md
@src/aurora/cli/app.py
@src/aurora/runtime/paths.py
@src/aurora/runtime/settings.py
@tests/cli/test_entrypoint.py
@tests/runtime/test_settings_defaults.py

<interfaces>
From src/aurora/cli/app.py:
```python
app.add_typer(setup_app, name="setup")
app.add_typer(config_app, name="config")
app.add_typer(model_app, name="model")
app.add_typer(doctor_app, name="doctor")
```

From src/aurora/runtime/settings.py:
```python
class RuntimeSettings(BaseSettings): ...
def load_settings() -> RuntimeSettings: ...
def save_settings(settings: RuntimeSettings) -> RuntimeSettings: ...
```

From src/aurora/runtime/paths.py:
```python
def get_config_dir() -> Path: ...
def ensure_config_dir() -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define KB contracts used by all lifecycle commands</name>
  <files>src/aurora/kb/contracts.py, src/aurora/kb/__init__.py, tests/kb/test_contracts.py</files>
  <behavior>
    - Contract models represent scope rules, per-file diagnostics, staged counters, and final operation summary.
    - One summary object can be serialized deterministically for both human text rendering and `--json`.
    - Contract tests fail when counters or required fields drift.
  </behavior>
  <action>Create `src/aurora/kb/contracts.py` with typed models for scope config (vault root, includes, excludes, default exclusions), operation counters (`read/indexed/updated/removed/skipped/errors`), and structured per-file errors (`path`, `category`, `recovery_hint`, without note content). Export the contracts from `src/aurora/kb/__init__.py` and add unit tests that validate normalization and deterministic `model_dump`/JSON behavior.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_contracts.py -q</automated>
  </verify>
  <done>KB contracts are stable, serializable, and ready for scanner/manifest/service integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend global runtime settings and paths for KB scope persistence</name>
  <files>src/aurora/runtime/paths.py, src/aurora/runtime/settings.py, tests/runtime/test_settings_defaults.py</files>
  <behavior>
    - KB settings persist globally with deterministic JSON like existing runtime settings.
    - Persisted scope fields support include/exclude lists and default hidden/system/junk exclusions.
    - Loading malformed KB settings yields actionable errors instead of silent fallback.
  </behavior>
  <action>Add dedicated KB paths (for manifest/state files) in `runtime/paths.py`. Extend `RuntimeSettings` with KB scope fields used in Phase 2 (`kb_vault_path`, `kb_include`, `kb_exclude`, `kb_default_excludes`) while preserving Phase 1 defaults. Keep policy enforcement local-only and update serialization tests in `tests/runtime/test_settings_defaults.py` to validate round-trip persistence and sorted-key output with new KB fields.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_settings_defaults.py -q</automated>
  </verify>
  <done>Runtime settings now persist KB scope defaults globally and deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `aurora kb` CLI namespace with command signatures and shared output contract</name>
  <files>src/aurora/cli/kb.py, src/aurora/cli/app.py, tests/cli/test_entrypoint.py</files>
  <behavior>
    - Root CLI help lists `kb` alongside existing groups.
    - `aurora kb ingest <vault_path>` requires explicit vault path argument.
    - `ingest`, `update`, `delete`, and `rebuild` expose `--json` (plus `--dry-run` where applicable) and fail fast when called before implementation wiring.
  </behavior>
  <action>Create `src/aurora/cli/kb.py` with Typer commands for `ingest`, `update`, `delete`, and `rebuild`, each typed to emit the shared KB summary contract. Register `kb_app` in `src/aurora/cli/app.py`. Implement temporary fail-fast handlers that return structured actionable diagnostics (pt-BR + recovery guidance) rather than placeholder "not implemented" text, so downstream plans can swap only service wiring.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_entrypoint.py -q -k "kb or command_groups"</automated>
  </verify>
  <done>`aurora kb` namespace and command contracts are visible and stable for Wave 2 implementation.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kb/test_contracts.py -q` passes.
- `uv run pytest tests/runtime/test_settings_defaults.py -q` passes with KB fields in round-trip.
- `uv run pytest tests/cli/test_entrypoint.py -q -k "kb or command_groups"` passes and root help includes `kb`.
</verification>

<success_criteria>
- Phase 2 implementation has a locked contract layer (CLI + settings + summary schema) with no ambiguity.
- KB scope persistence is ready before scope/delta algorithms are implemented.
- Wave 2 plans can run in parallel without interface-level conflicts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-knowledge-base-lifecycle/02-01-SUMMARY.md`
</output>
