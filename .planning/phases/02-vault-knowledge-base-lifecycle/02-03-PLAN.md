---
phase: 02-vault-knowledge-base-lifecycle
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/aurora/kb/delta.py
  - src/aurora/kb/manifest.py
  - src/aurora/kb/qmd_adapter.py
  - tests/kb/test_delta.py
  - tests/kb/test_manifest.py
  - tests/kb/test_qmd_adapter.py
autonomous: true
requirements:
  - KB-02
  - KB-03
  - KB-04
  - KB-05
  - PRIV-02
must_haves:
  truths:
    - "Incremental update processes only changed notes by default, using mtime+size with optional hash verification."
    - "Notes removed from vault are automatically removed from KB state/index during update, and explicit delete removes selected notes from index state."
    - "When manifest/index state diverges or is corrupted, operation fails fast with explicit rebuild guidance."
  artifacts:
    - path: "src/aurora/kb/manifest.py"
      provides: "Persistent KB manifest with deterministic serialization and fingerprint metadata"
      contains: "size, mtime, optional hash, and indexed note records"
    - path: "src/aurora/kb/delta.py"
      provides: "Delta classifier for added/updated/removed/unchanged note sets"
      contains: "mtime+size baseline with optional hash refinement path"
    - path: "src/aurora/kb/qmd_adapter.py"
      provides: "Adapter boundary for index apply/remove/rebuild operations"
      contains: "explicit delete and rebuild hooks with typed diagnostics"
  key_links:
    - from: "src/aurora/kb/delta.py"
      to: "src/aurora/kb/manifest.py"
      via: "Delta logic compares current scan fingerprints with persisted manifest"
      pattern: "mtime|size|sha256"
    - from: "src/aurora/kb/qmd_adapter.py"
      to: "src/aurora/kb/manifest.py"
      via: "Successful adapter operations commit manifest updates; failed operations do not mutate state"
      pattern: "apply|delete|rebuild|commit"
    - from: "src/aurora/kb/delta.py"
      to: "src/aurora/kb/contracts.py"
      via: "Classified changes feed operation counters and final summary"
      pattern: "updated|removed|skipped|errors"
---

<objective>
Implement deterministic KB state management for update/delete/rebuild semantics before CLI wiring.

Purpose: Make incremental and destructive lifecycle commands predictable and recoverable under divergence/corruption scenarios.
Output: Manifest persistence, delta classification, and QMD adapter boundaries validated with unit tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-CONTEXT.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-RESEARCH.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-01-PLAN.md
@src/aurora/kb/contracts.py
@src/aurora/runtime/errors.py
@src/aurora/runtime/paths.py
@src/aurora/runtime/server_state.py

<interfaces>
From src/aurora/runtime/server_state.py:
```python
def load_server_state() -> ServerLifecycleState | None: ...
def save_server_state(state: ServerLifecycleState) -> ServerLifecycleState: ...
class ServerStateError(Exception): ...
```

From src/aurora/runtime/errors.py:
```python
class RuntimeDiagnosticError(Exception): ...
def build_runtime_error(category: RuntimeErrorCategory, ...) -> RuntimeDiagnosticError: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manifest persistence with deterministic fingerprint schema</name>
  <files>src/aurora/kb/manifest.py, tests/kb/test_manifest.py</files>
  <behavior>
    - Manifest stores per-note fingerprint metadata (`size`, `mtime_ns`, optional `sha256`) and indexing trace fields.
    - Manifest serialization is deterministic and stable across runs.
    - Corrupt manifest payloads fail with actionable rebuild guidance.
  </behavior>
  <action>Implement `manifest.py` with typed load/save APIs and strict payload validation inspired by `server_state.py`. Persist note records keyed by vault-relative path, include preprocessing trace metadata, and ensure deterministic JSON output. Raise typed KB state errors that include `aurora kb rebuild` recovery commands when manifest content is invalid or incompatible.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_manifest.py -q</automated>
  </verify>
  <done>KB manifest can be safely loaded/saved and reports actionable failures on corruption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement incremental delta classifier for update/delete semantics</name>
  <files>src/aurora/kb/delta.py, tests/kb/test_delta.py</files>
  <behavior>
    - Delta classification identifies added/changed/removed/unchanged notes from scanner + manifest state.
    - Default change detection uses mtime+size; optional strict mode verifies hash for changed candidates.
    - Removed files discovered during update are emitted as index removals automatically.
  </behavior>
  <action>Create delta utilities that compare current scanner results against manifest records and produce deterministic operation sets used by `kb update` and `kb rebuild`. Include strict hash verification mode for precision on demand, ensure scope filtering still applies during update, and surface divergence flags when manifest references impossible or conflicting state.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_delta.py -q</automated>
  </verify>
  <done>Incremental update semantics are deterministic and satisfy changed/removed detection requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create QMD adapter boundary for apply/remove/rebuild index operations</name>
  <files>src/aurora/kb/qmd_adapter.py, tests/kb/test_qmd_adapter.py</files>
  <behavior>
    - Adapter supports targeted apply/delete operations and full rebuild entrypoint.
    - Adapter errors map to typed categories with recovery hints, not raw subprocess traces.
    - Adapter contract keeps Aurora lifecycle semantics independent from upstream QMD command drift.
  </behavior>
  <action>Implement `qmd_adapter.py` as a thin boundary that translates Aurora delta/rebuild requests into backend operations, normalizes backend responses into typed results, and preserves no-content error diagnostics (`path`, `category`, `recovery_hint`). Add unit tests with command stubs/fakes for success, partial failure, and corruption/divergence cases where adapter must refuse state mutation.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_qmd_adapter.py -q</automated>
  </verify>
  <done>Index backend integration is isolated behind a stable adapter contract ready for service orchestration.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kb/test_manifest.py -q` passes.
- `uv run pytest tests/kb/test_delta.py -q` passes.
- `uv run pytest tests/kb/test_qmd_adapter.py -q` passes.
</verification>

<success_criteria>
- Manifest and delta logic enforce deterministic incremental behavior.
- Delete and rebuild pathways are explicit and safe under divergence.
- Backend index operations are adapter-isolated so lifecycle semantics remain Aurora-owned.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-knowledge-base-lifecycle/02-03-SUMMARY.md`
</output>
