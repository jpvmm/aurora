---
phase: 02-vault-knowledge-base-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/aurora/kb/preprocess.py
  - src/aurora/kb/scanner.py
  - src/aurora/kb/scope.py
  - tests/kb/test_preprocess.py
  - tests/kb/test_scanner.py
  - tests/kb/test_scope.py
autonomous: true
requirements:
  - KB-01
  - KB-05
  - PRIV-02
must_haves:
  truths:
    - "Only in-vault `.md` notes matching include/exclude rules are eligible for indexing."
    - "When include/exclude rules conflict, excludes always win and out-of-vault includes are blocked with explicit guidance."
    - "Templater snippets are stripped before indexing and cleaning counts are reported without exposing note content."
  artifacts:
    - path: "src/aurora/kb/scope.py"
      provides: "Case-sensitive scope engine with relative-path enforcement and dry-run preview"
      contains: "include/exclude evaluation where exclude precedence is explicit"
    - path: "src/aurora/kb/scanner.py"
      provides: "Vault scanner that enumerates eligible `.md` files while ignoring symlinks"
      contains: "pathlib-based traversal returning vault-relative paths"
    - path: "src/aurora/kb/preprocess.py"
      provides: "Templater-cleaning pipeline with traceability metadata"
      contains: "cleaned text plus cleaned snippet counts"
  key_links:
    - from: "src/aurora/kb/scanner.py"
      to: "src/aurora/kb/scope.py"
      via: "Every discovered markdown path is filtered by case-sensitive scope rules"
      pattern: "fnmatchcase|should_index|exclude"
    - from: "src/aurora/kb/preprocess.py"
      to: "src/aurora/kb/contracts.py"
      via: "Preprocessing emits structured metadata consumed by operation summaries"
      pattern: "cleaned_count|cleaned_spans"
    - from: "src/aurora/kb/scope.py"
      to: "src/aurora/runtime/settings.py"
      via: "Persisted include/exclude/default scope values are resolved relative to vault root"
      pattern: "kb_include|kb_exclude|kb_default_excludes"
---

<objective>
Implement scoped markdown discovery and templater cleaning rules that all KB lifecycle commands rely on.

Purpose: Enforce the privacy and scope contract before any index mutation logic runs.
Output: Scope evaluator, scanner, and preprocessor modules with focused unit tests.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-CONTEXT.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-RESEARCH.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-01-PLAN.md
@src/aurora/kb/contracts.py
@src/aurora/runtime/settings.py

<interfaces>
From src/aurora/kb/contracts.py:
```python
class KBScopeConfig(...): ...
class KBOperationCounters(...): ...
class KBOperationSummary(...): ...
```

From src/aurora/runtime/settings.py:
```python
class RuntimeSettings(BaseSettings):
    kb_vault_path: str | None
    kb_include: list[str]
    kb_exclude: list[str]
    kb_default_excludes: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scope rule engine with vault-boundary enforcement and dry-run preview</name>
  <files>src/aurora/kb/scope.py, tests/kb/test_scope.py</files>
  <behavior>
    - Includes/excludes accept exact relative paths and glob patterns with case-sensitive matching.
    - Exclude precedence wins over include when both match.
    - Includes outside vault root fail with clear diagnostics and recovery guidance.
  </behavior>
  <action>Implement scope parsing and matching in `src/aurora/kb/scope.py` using vault-root normalization (`Path.resolve`, `is_relative_to`) and `fnmatchcase` for cross-platform case-sensitive behavior. Enforce first-run default excludes for hidden/system/junk folders, reject out-of-vault include rules, and add dry-run preview output with effective scope details plus zero-match failure handling.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_scope.py -q</automated>
  </verify>
  <done>Scope rules are deterministic, case-sensitive, and safe against path-escape indexing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement markdown scanner constrained to scope and privacy defaults</name>
  <files>src/aurora/kb/scanner.py, tests/kb/test_scanner.py</files>
  <behavior>
    - Scanner considers only `.md` files relative to vault root.
    - Symlinks are ignored by default.
    - Scanner reports skipped reasons (scope, extension, hidden/system exclusion) without reading note content.
  </behavior>
  <action>Create scanner utilities that traverse the vault root, emit normalized relative paths, and classify skipped candidates for progress counters. Ensure scanner behavior is scope-aware by delegating every candidate to the scope engine and returning deterministic ordering for stable update/rebuild behavior.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_scanner.py -q</automated>
  </verify>
  <done>Scanner output is deterministic and restricted to in-scope markdown files with privacy-safe skip diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add templater pre-processing with traceability metadata</name>
  <files>src/aurora/kb/preprocess.py, tests/kb/test_preprocess.py</files>
  <behavior>
    - Common Templater blocks (`&lt;% ... %&gt;`, `&lt;%* ... %&gt;`, related variants) are removed before indexing.
    - Preprocessing returns cleaned text plus snippet-cleaning metadata for reporting.
    - Logs/diagnostics avoid raw markdown content.
  </behavior>
  <action>Implement a preprocessor that strips templater syntax while preserving file traceability metadata (relative path, cleaned snippet count, cleaned span count). Keep output contract stable for ingest/update/rebuild reuse and add tests for no-template files, multi-template notes, and mixed content where only templater snippets are removed.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_preprocess.py -q</automated>
  </verify>
  <done>Templater cleanup is consistent and reusable across all lifecycle commands without leaking note content in logs.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kb/test_scope.py -q` passes.
- `uv run pytest tests/kb/test_scanner.py -q` passes.
- `uv run pytest tests/kb/test_preprocess.py -q` passes.
</verification>

<success_criteria>
- Scope evaluation is deterministic, secure, and aligned with locked decision rules.
- Eligible markdown files can be enumerated consistently for ingest/update/rebuild.
- Preprocessed payloads are retrieval-ready while preserving operational traceability and privacy boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-knowledge-base-lifecycle/02-02-SUMMARY.md`
</output>
