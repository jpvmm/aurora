---
phase: 02-vault-knowledge-base-lifecycle
plan: 06
type: execute
wave: 5
depends_on:
  - "02-05"
files_modified:
  - pyproject.toml
  - tests/cli/test_kb_command.py
  - tests/integration/test_kb_qmd_integration.py
  - tests/integration/conftest.py
autonomous: true
gap_closure: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04
  - KB-05
  - PRIV-02
must_haves:
  truths:
    - "KB CLI stage/progress/final-summary output remains readable and stable for terminal users without source inspection."
    - "A real QMD index run proves ingest/update/delete/rebuild effects end-to-end in backend state, not only Aurora manifest state."
    - "Integration verification remains local-only and scope-aware, with no note-content leakage in assertion output."
  artifacts:
    - path: "tests/cli/test_kb_command.py"
      provides: "Terminal readability contract coverage for KB lifecycle logs and summaries"
      contains: "stage order assertions and stable summary field expectations"
    - path: "tests/integration/test_kb_qmd_integration.py"
      provides: "Real backend integration verification for full lifecycle"
      contains: "ingest/update/delete/rebuild assertions against isolated qmd index/collection"
    - path: "tests/integration/conftest.py"
      provides: "Isolation and cleanup fixtures for qmd-backed integration runs"
      contains: "temp index naming, collection teardown, environment setup"
  key_links:
    - from: "tests/cli/test_kb_command.py"
      to: "src/aurora/cli/kb.py"
      via: "CliRunner output contract checks"
      pattern: "etapa: scan|etapa: preprocess|etapa: done|totais:"
    - from: "tests/integration/test_kb_qmd_integration.py"
      to: "src/aurora/kb/qmd_backend.py"
      via: "integration flow invoking real qmd transport"
      pattern: "qmd|collection|update|remove|rebuild"
    - from: "tests/integration/test_kb_qmd_integration.py"
      to: "src/aurora/kb/service.py"
      via: "aurora kb command invocation through production lifecycle path"
      pattern: "kb ingest|kb update|kb delete|kb rebuild"
---

<objective>
Close the remaining Phase 2 verification gaps by replacing manual-only checks with deterministic automated verification for CLI readability and real backend behavior.

Purpose: Move Phase 2 from "human_needed" to reproducible automated confidence for lifecycle UX and backend effects.
Output: Integration and CLI contract tests that prove user-visible logs and real QMD lifecycle mutations.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-CONTEXT.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-VERIFICATION.md
@.planning/phases/02-vault-knowledge-base-lifecycle/02-05-PLAN.md
@src/aurora/cli/kb.py
@src/aurora/kb/service.py
@src/aurora/kb/qmd_backend.py
@tests/cli/test_kb_command.py

<interfaces>
From src/aurora/cli/kb.py:
```python
def kb_ingest_command(vault_path: str, json_output: bool = False, dry_run: bool = False) -> None: ...
def kb_update_command(json_output: bool = False, dry_run: bool = False, verify_hash: bool = False) -> None: ...
def kb_delete_command(json_output: bool = False) -> None: ...
def kb_rebuild_command(json_output: bool = False, dry_run: bool = False) -> None: ...
```

From src/aurora/kb/contracts.py:
```python
class KBOperationSummary(BaseModel):
    operation: Literal["ingest", "update", "delete", "rebuild"]
    counters: KBOperationCounters
    scope: KBScopeConfig
    diagnostics: tuple[KBFileDiagnostic, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock terminal readability contract for KB lifecycle logs and summaries</name>
  <files>tests/cli/test_kb_command.py</files>
  <behavior>
    - Text mode output always includes ordered stages (`scan`, `preprocess`, `done`) and one deterministic final summary block.
    - Final summary always includes operation, duration, effective scope, and totals counters.
    - JSON mode remains machine-stable and does not include transient progress lines.
  </behavior>
  <action>Add focused CLI output contract tests that normalize ANSI escape noise and assert stage ordering, counter visibility, and summary readability tokens for ingest/update/delete/rebuild. Keep assertions privacy-safe by checking for required operational fields and explicit absence of raw note-body fixture strings in diagnostics output. Do not widen scope into UI redesign; this task only codifies readability baseline contract.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kb_command.py -q -k "kb and (progress or summary or json or readability)"</automated>
  </verify>
  <done>Readability expectations are executable and regression-protected through deterministic CLI contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real QMD integration suite for ingest/update/delete/rebuild lifecycle</name>
  <files>pyproject.toml, tests/integration/conftest.py, tests/integration/test_kb_qmd_integration.py</files>
  <behavior>
    - Integration tests run `aurora kb` commands against a temporary vault and isolated qmd index/collection identifiers.
    - Tests verify backend-visible lifecycle effects across ingest/update/delete/rebuild (file presence and count changes in qmd collection state).
    - Tests perform deterministic cleanup of created qmd collections/index references at teardown.
  </behavior>
  <action>Create integration fixtures that provision temporary Aurora config + vault directories, unique qmd index/collection names, and cleanup hooks that remove test collections after each run. Implement end-to-end tests invoking CLI commands and asserting qmd state transitions (initial ingest entries, update reflects file edits/removals, delete clears in-scope entries, rebuild restores current scoped notes). Mark tests with an explicit `integration` marker and keep output assertions privacy-safe (paths/categories only).</action>
  <verify>
    <automated>uv run pytest tests/integration/test_kb_qmd_integration.py -q</automated>
  </verify>
  <done>Phase 2 lifecycle behavior is proven against a real qmd backend flow with repeatable local integration tests.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/cli/test_kb_command.py -q -k "kb and (progress or summary or json or readability)"` passes.
- `uv run pytest tests/integration/test_kb_qmd_integration.py -q` passes.
</verification>

<success_criteria>
- Verification no longer depends on subjective/manual checks for CLI readability or backend mutation visibility.
- Real qmd integration tests prove end-to-end lifecycle correctness for KB-01 through KB-04 under scoped rules.
- CLI output contracts preserve KB-05 and PRIV-02 expectations across future regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault-knowledge-base-lifecycle/02-06-SUMMARY.md`
</output>
