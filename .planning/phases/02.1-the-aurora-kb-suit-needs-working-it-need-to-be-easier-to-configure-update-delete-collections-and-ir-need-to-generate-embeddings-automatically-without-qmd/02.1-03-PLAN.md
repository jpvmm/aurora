---
phase: 02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd
plan: 03
type: execute
wave: 3
depends_on:
  - "02.1-01"
  - "02.1-02"
files_modified:
  - src/aurora/runtime/settings.py
  - src/aurora/kb/lock.py
  - src/aurora/kb/scheduler_state.py
  - src/aurora/kb/scheduler.py
  - src/aurora/kb/service.py
  - src/aurora/cli/kb.py
  - tests/kb/test_kb_lock.py
  - tests/kb/test_kb_scheduler.py
  - tests/cli/test_kb_command.py
autonomous: true
requirements:
  - KB-02
  - CLI-02
must_haves:
  truths:
    - "Scheduler is disabled by default and can be explicitly enabled/disabled/inspected via CLI."
    - "Scheduled KB update runs daily at configured local hour, and when a run is missed because machine was off, one catch-up run occurs on next boot before returning to normal cadence."
    - "KB mutations (manual and scheduled) never execute concurrently due to one shared global lock contract."
  artifacts:
    - path: "src/aurora/kb/scheduler.py"
      provides: "Deterministic scheduler service for due-check and catch-up behavior"
      contains: "enable/disable/status/run_due orchestration"
    - path: "src/aurora/kb/lock.py"
      provides: "Cross-terminal KB mutation lock shared by manual and scheduler-triggered flows"
      contains: "acquire_kb_lock and timeout/stale-lock handling"
    - path: "src/aurora/cli/kb.py"
      provides: "Scheduler lifecycle command surface and concise operational status output"
      contains: "kb scheduler enable/disable/status commands and run integration"
  key_links:
    - from: "src/aurora/kb/scheduler.py"
      to: "src/aurora/kb/service.py"
      via: "scheduler-triggered update path using same lifecycle orchestration as manual runs"
      pattern: "run_update|run_due|catch_up"
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/kb/lock.py"
      via: "single lock wraps all mutating KB operations"
      pattern: "acquire_kb_lock"
    - from: "src/aurora/cli/kb.py"
      to: "src/aurora/kb/scheduler_state.py"
      via: "scheduler status rendering from persisted state/next run info"
      pattern: "status|next_run|last_run"
---

<objective>
Deliver scheduler and operational control for periodic KB updates with robust concurrency safety and missed-run catch-up behavior.

Purpose: Let users opt into automatic daily freshness while preventing race conditions between scheduled and manual KB mutations.
Output: KB lock primitive, scheduler state/service modules, and CLI lifecycle commands (`enable/disable/status`) with full regression coverage.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-CONTEXT.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-RESEARCH.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-01-PLAN.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-02-PLAN.md
@src/aurora/runtime/paths.py
@src/aurora/runtime/server_lock.py
@src/aurora/kb/service.py
@src/aurora/cli/kb.py

<requirement_gap>
Official requirement mapping in this plan:
- `KB-02`: periodic scheduled update maintains incremental freshness behavior with one missed-run catch-up.
- `CLI-02`: scheduler lifecycle control is exposed through explicit CLI commands (`enable/disable/status`).
</requirement_gap>

<interfaces>
From src/aurora/runtime/server_lock.py:
```python
@contextmanager
def acquire_lifecycle_lock(
    *,
    timeout_seconds: float = 5.0,
    poll_interval: float = 0.1,
) -> Iterator[Path]: ...
```

From src/aurora/kb/service.py:
```python
class KBService:
    def run_update(...): ...
    def run_delete(...): ...
    def run_rebuild(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared KB mutation lock and enforce it across all mutating service paths</name>
  <files>src/aurora/kb/lock.py, src/aurora/kb/service.py, tests/kb/test_kb_lock.py, tests/runtime/test_kb_service.py</files>
  <behavior>
    - One global lock serializes ingest/update/delete/rebuild regardless of trigger source.
    - Lock acquisition has deterministic timeout and stale-lock recovery semantics.
    - Scheduler and manual commands share the exact same lock contract.
  </behavior>
  <action>Create KB-specific lock primitive by reusing proven lifecycle lock design (token ownership, timeout, stale PID recovery). Wrap all mutating `KBService` operations with this lock, returning actionable diagnostics when lock cannot be acquired. Ensure behavior is deterministic (queue/wait then fail with clear recovery if timeout) and add tests for contention and stale lock reclaim.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_kb_lock.py tests/runtime/test_kb_service.py -q -k "lock or timeout or stale"</automated>
  </verify>
  <done>No KB mutation path can run concurrently without explicit serialized lock ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scheduler state + due engine with daily hour policy and boot catch-up</name>
  <files>src/aurora/runtime/settings.py, src/aurora/kb/scheduler_state.py, src/aurora/kb/scheduler.py, tests/kb/test_kb_scheduler.py</files>
  <behavior>
    - Scheduler is disabled by default and stores deterministic state for last scheduled slot/run metadata.
    - Due calculation uses configured local hour and timezone-aware timestamps.
    - Missed scheduled run while machine is off triggers one catch-up on next boot, then resumes daily cadence.
  </behavior>
  <action>Add scheduler configuration/state contracts for daily local-hour execution and persisted bookkeeping (last planned slot, last success/failure, next due). Implement `KBSchedulerService` with `enable`, `disable`, `status`, and `run_due` logic that uses shared `KBService` update orchestration and lock semantics. Keep scheduling model intentionally simple (fixed daily hour, no cron DSL) and cover defaults plus DST/local-time edge behavior with deterministic scheduler tests.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_kb_scheduler.py -q -k "scheduler or daily or catch"</automated>
  </verify>
  <done>Scheduler logic is deterministic, persisted, and honors daily cadence with single catch-up semantics after downtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose scheduler lifecycle commands in CLI with concise status and recovery guidance</name>
  <files>src/aurora/cli/kb.py, tests/cli/test_kb_command.py</files>
  <behavior>
    - CLI exposes `aurora kb scheduler enable`, `disable`, and `status`.
    - Status output clearly shows enabled state, configured hour, next due time, and last run result.
    - Command outputs remain concise and include explicit recovery/next-step guidance when blocked or misconfigured.
  </behavior>
  <action>Add scheduler command group under `aurora kb` that delegates to `KBSchedulerService`. Keep renderers deterministic in text and JSON and include required operational fields (enabled, local hour, next_due, last_run outcome, catch-up eligibility). Add behavior tests for enable/disable/status across configured/unconfigured KB states.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kb_command.py -q -k "kb scheduler or next_due or catch_up"</automated>
  </verify>
  <done>Users can control and inspect scheduler behavior directly from CLI with clear, stable output contracts.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kb/test_kb_lock.py tests/runtime/test_kb_service.py -q -k "lock"` passes.
- `uv run pytest tests/kb/test_kb_scheduler.py -q -k "scheduler or catch"` passes.
- `uv run pytest tests/cli/test_kb_command.py -q -k "kb scheduler"` passes.
</verification>

<success_criteria>
- Scheduler is opt-in, daily-local-hour based, and operationally visible via explicit CLI commands.
- Missed scheduled updates produce one deterministic catch-up on next boot.
- Manual and scheduled KB mutations share one global lock and cannot overlap.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-03-SUMMARY.md`
</output>
