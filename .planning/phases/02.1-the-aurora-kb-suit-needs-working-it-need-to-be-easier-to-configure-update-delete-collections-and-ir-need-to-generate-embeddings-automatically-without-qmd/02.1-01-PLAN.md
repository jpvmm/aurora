---
phase: 02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aurora/runtime/settings.py
  - src/aurora/cli/config.py
  - src/aurora/cli/kb.py
  - src/aurora/kb/service.py
  - src/aurora/kb/qmd_backend.py
  - tests/runtime/test_settings_defaults.py
  - tests/cli/test_config_show.py
  - tests/cli/test_kb_command.py
  - tests/runtime/test_kb_service.py
autonomous: true
requirements:
  - CLI-02
  - KB-03
must_haves:
  truths:
    - "User can configure and inspect KB runtime settings via a dedicated `aurora kb config` surface without editing JSON files."
    - "Update/delete/rebuild use one active global index/collection by default, but user can override target per command without mutating saved config."
    - "Destructive `aurora kb delete` requires explicit confirmation and supports non-interactive `--yes` flow."
  artifacts:
    - path: "src/aurora/cli/kb.py"
      provides: "KB config command surface and safer lifecycle command options"
      contains: "kb config show/set handlers, delete confirmation, per-command target overrides"
    - path: "src/aurora/runtime/settings.py"
      provides: "Persisted KB operational settings for active target and auto-embedding toggle"
      contains: "validated KB config fields with deterministic serialization"
    - path: "src/aurora/cli/config.py"
      provides: "Global config view including KB settings summary"
      contains: "vault/index/collection/auto-embeddings rendering"
  key_links:
    - from: "src/aurora/cli/kb.py"
      to: "src/aurora/runtime/settings.py"
      via: "kb config read/write and command default target resolution"
      pattern: "load_settings|save_settings|kb_.*index|kb_.*collection"
    - from: "src/aurora/cli/kb.py"
      to: "src/aurora/kb/service.py"
      via: "command-level overrides passed as non-persisting runtime options"
      pattern: "run_update|run_delete|run_rebuild"
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/kb/qmd_backend.py"
      via: "effective target identifiers flow into backend transport"
      pattern: "QMDCliBackend|index_name|collection_name"
---

<objective>
Establish the KB configuration and operation UX foundation: dedicated `aurora kb config`, visible KB settings in `aurora config show`, safe delete flow, and per-command index/collection overrides.

Purpose: Make KB lifecycle operations easy and safe before adding scheduler and auto-embedding orchestration.
Output: CLI/config/service contracts for active-target defaults, override behavior, and confirmation semantics.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-CONTEXT.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-RESEARCH.md
@src/aurora/cli/kb.py
@src/aurora/cli/config.py
@src/aurora/runtime/settings.py
@src/aurora/kb/service.py
@src/aurora/kb/qmd_backend.py
@tests/cli/test_kb_command.py
@tests/cli/test_config_show.py

<requirement_gap>
Official requirement mapping used by this plan:
- `CLI-02`: dedicated KB command surface (`aurora kb config`) and operational command UX.
- `KB-03`: deterministic delete behavior with explicit confirmation safety.
</requirement_gap>

<interfaces>
From src/aurora/runtime/settings.py:
```python
class RuntimeSettings(BaseSettings):
    kb_vault_path: str
    kb_qmd_index_name: str
    kb_qmd_collection_name: str
```

From src/aurora/cli/kb.py:
```python
def kb_update_command(json_output: bool = False, dry_run: bool = False, verify_hash: bool = False) -> None: ...
def kb_delete_command(json_output: bool = False) -> None: ...
def kb_rebuild_command(json_output: bool = False, dry_run: bool = False) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend persisted KB settings and global config rendering for operations UX</name>
  <files>src/aurora/runtime/settings.py, src/aurora/cli/config.py, tests/runtime/test_settings_defaults.py, tests/cli/test_config_show.py</files>
  <behavior>
    - Runtime settings include KB operation toggles required by phase decisions, including auto-embeddings control.
    - `aurora config show` displays KB-relevant summary (`vault/index/collection/auto-embeddings`) while preserving masking/privacy behavior.
    - New fields round-trip deterministically through JSON serialization and validation.
  </behavior>
  <action>Add validated KB operational settings (including auto-embedding enablement) to `RuntimeSettings` with deterministic persistence and defaults aligned to phase decisions. Extend `aurora config show` to print concise KB summary fields alongside runtime/privacy sections. Keep existing sensitive-value masking behavior unchanged and add/adjust tests for defaults, serialization, and rendered output contracts.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_settings_defaults.py tests/cli/test_config_show.py -q</automated>
  </verify>
  <done>KB settings needed by this phase are persisted, validated, and visible through stable CLI output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce `aurora kb config` command surface for active KB target management</name>
  <files>src/aurora/cli/kb.py, tests/cli/test_kb_command.py</files>
  <behavior>
    - `aurora kb config show` provides detailed KB view (vault/include/exclude/index/collection/auto-embeddings).
    - `aurora kb config set` updates active global target and related KB settings in one place.
    - Root and KB help output expose the new command surface clearly.
  </behavior>
  <action>Add a dedicated `kb config` sub-namespace under the existing `kb` Typer app, with explicit show/set commands for KB settings and active index/collection target. Keep one active target model globally as locked by user decisions. Reuse existing settings loaders/savers and maintain pt-BR actionable output style; add command/help behavior tests for discoverability and deterministic output in `test_kb_command.py`.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kb_command.py -q -k "kb config or kb help or kb command"</automated>
  </verify>
  <done>Users can configure and inspect KB operational settings through a dedicated command surface without manual file edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add safe delete confirmation and per-command non-persisting target overrides</name>
  <files>src/aurora/cli/kb.py, src/aurora/kb/service.py, src/aurora/kb/qmd_backend.py, tests/cli/test_kb_command.py, tests/runtime/test_kb_service.py</files>
  <behavior>
    - `update/delete/rebuild` default to active global target, with optional `--index`/`--collection` override for one command run.
    - `kb delete` requires interactive confirmation unless `--yes` is explicitly provided.
    - Non-interactive destructive delete without `--yes` fails with actionable recovery guidance.
  </behavior>
  <action>Thread optional index/collection overrides from CLI into service/backend construction so override scope is command-local (no implicit persistence). Add `--yes` confirmation pattern for delete, mirroring existing operational safety conventions. Keep output concise and include explicit next-step/recovery command text when blocked. Add regression tests for override propagation, default-target fallback, and delete confirmation behavior.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kb_command.py tests/runtime/test_kb_service.py -q -k "delete or yes or override or collection or index"</automated>
  </verify>
  <done>Update/delete/rebuild target resolution is predictable and safe, and delete cannot run destructively without explicit confirmation semantics.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_settings_defaults.py tests/cli/test_config_show.py -q` passes.
- `uv run pytest tests/cli/test_kb_command.py -q -k "kb config or kb help"` passes.
- `uv run pytest tests/cli/test_kb_command.py tests/runtime/test_kb_service.py -q -k "delete or yes or override"` passes.
</verification>

<success_criteria>
- KB configuration is first-class in CLI (`aurora kb config`) and visible in `aurora config show`.
- Active global target behavior is explicit, with optional command-scoped overrides for update/delete/rebuild.
- Destructive delete flow is guarded by interactive confirmation or explicit `--yes`.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-01-SUMMARY.md`
</output>
