---
phase: 02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd
plan: 02
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - src/aurora/kb/contracts.py
  - src/aurora/kb/qmd_adapter.py
  - src/aurora/kb/qmd_backend.py
  - src/aurora/kb/service.py
  - src/aurora/cli/kb.py
  - tests/runtime/test_kb_service.py
  - tests/kb/test_qmd_backend.py
  - tests/kb/test_qmd_adapter.py
  - tests/cli/test_kb_command.py
autonomous: true
requirements:
  - KB-02
  - KB-05
must_haves:
  truths:
    - "Auto-embeddings execute automatically only when KB mutation actually changes indexed content (delta-gated behavior)."
    - "KB commands remain blocking by default: operation completes only after embedding step succeeds or reports partial failure."
    - "When embedding fails, user receives explicit partial-failure warning with concrete recovery command, while index mutation result remains visible."
  artifacts:
    - path: "src/aurora/kb/service.py"
      provides: "Lifecycle orchestration that runs embed stage conditionally and tracks partial failures"
      contains: "delta-gated embed trigger and summary/warning propagation"
    - path: "src/aurora/kb/qmd_backend.py"
      provides: "QMD transport support for explicit embedding command"
      contains: "embed command execution and typed diagnostic mapping"
    - path: "src/aurora/cli/kb.py"
      provides: "Text/JSON rendering that exposes embedding stage outcome and recovery guidance"
      contains: "partial-failure warning path and deterministic output fields"
  key_links:
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/kb/qmd_backend.py"
      via: "post-mutation embed call when state_mutated and auto-embedding enabled"
      pattern: "embed|auto_embeddings|state_mutated"
    - from: "src/aurora/kb/service.py"
      to: "src/aurora/cli/kb.py"
      via: "operation summary carries embedding result for text/json output"
      pattern: "embedding|warning|recovery"
    - from: "tests/runtime/test_kb_service.py"
      to: "src/aurora/kb/qmd_adapter.py"
      via: "tests prove embed never runs when no delta mutation occurred"
      pattern: "unchanged|dry_run|state_mutated"
---

<objective>
Implement automatic embedding orchestration directly in Aurora KB lifecycle operations, with delta-gated execution and explicit partial-failure contracts.

Purpose: Remove manual `qmd embed` burden while preserving deterministic, safe command semantics and user-visible recovery guidance.
Output: Service/backend/CLI contracts for embed stage execution, reporting, and regression coverage.
</objective>

<execution_context>
@/Users/joao.marinho/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joao.marinho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-CONTEXT.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-RESEARCH.md
@.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-01-PLAN.md
@src/aurora/kb/contracts.py
@src/aurora/kb/qmd_adapter.py
@src/aurora/kb/qmd_backend.py
@src/aurora/kb/service.py
@src/aurora/cli/kb.py

<requirement_gap>
Official requirement mapping used in this plan:
- `KB-02`: incremental maintenance flow is preserved while embedding refresh becomes automatic after true delta updates.
- `KB-05`: command outcomes remain explicit/readable with embedding-stage diagnostics and recovery guidance.
</requirement_gap>

<interfaces>
From src/aurora/kb/qmd_adapter.py:
```python
class QMDBackend(Protocol):
    def apply(self, notes: tuple[KBPreparedNote, ...]) -> QMDBackendResponse: ...
    def remove(self, paths: tuple[str, ...]) -> QMDBackendResponse: ...
    def rebuild(self, notes: tuple[KBPreparedNote, ...]) -> QMDBackendResponse: ...
```

From src/aurora/kb/contracts.py:
```python
class KBOperationSummary(BaseModel):
    operation: Literal["ingest", "update", "delete", "rebuild"]
    counters: KBOperationCounters
    diagnostics: tuple[KBFileDiagnostic, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend KB contracts and backend protocol to represent embedding stage outcomes</name>
  <files>src/aurora/kb/contracts.py, src/aurora/kb/qmd_adapter.py, src/aurora/kb/qmd_backend.py, tests/kb/test_qmd_backend.py, tests/kb/test_qmd_adapter.py</files>
  <behavior>
    - Backend protocol exposes explicit embedding operation with typed diagnostics.
    - Operation summary contract can represent embedding attempted/succeeded/failed state and recovery command.
    - JSON serialization remains deterministic and backwards-compatible for existing fields.
  </behavior>
  <action>Add explicit embedding response contract at backend/adapter boundaries (including diagnostic categories) and extend KB summary model with embedding status metadata. Implement `qmd embed` transport in `QMDCliBackend` with privacy-safe error mapping, keeping existing update/remove/rebuild semantics intact. Preserve deterministic serialization and add focused unit tests for embed command path and failure mapping.</action>
  <verify>
    <automated>uv run pytest tests/kb/test_qmd_backend.py tests/kb/test_qmd_adapter.py -q -k "embed or backend"</automated>
  </verify>
  <done>Codebase has typed, test-covered contracts for embedding step execution and diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement delta-gated blocking auto-embedding flow in KB service orchestration</name>
  <files>src/aurora/kb/service.py, tests/runtime/test_kb_service.py</files>
  <behavior>
    - Embed stage runs only when operation mutates indexed state and auto-embedding is enabled in settings.
    - Dry-run and no-delta operations do not trigger embedding.
    - Embedding failure is captured as partial failure (index mutation may already be complete) with deterministic diagnostics.
  </behavior>
  <action>Update service lifecycle methods to invoke embedding after successful mutating operations when `state_mutated` is true and config toggle enables auto-embeddings. Keep command flow blocking by default so completion reflects embed stage result. Encode partial-failure metadata into operation summary rather than silently swallowing errors; include recovery command guidance and ensure no note content leakage in messages. Add service tests for delta/no-delta, dry-run, success, and partial failure paths.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_kb_service.py -q -k "embed or auto_embeddings or partial"</automated>
  </verify>
  <done>Auto-embeddings are deterministic, conditional, and blocking, with explicit partial-failure semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render concise embed-stage status and recovery guidance in CLI text/JSON outputs</name>
  <files>src/aurora/cli/kb.py, tests/cli/test_kb_command.py</files>
  <behavior>
    - Text output clearly distinguishes full success vs partial success with embedding warning.
    - JSON output includes machine-readable embedding stage status and recovery command fields.
    - Existing readability contracts remain stable for stage ordering and totals.
  </behavior>
  <action>Update KB CLI renderers to include embedding stage outcome consistently across ingest/update/delete/rebuild where applicable. Keep output concise and actionable: explicit warning when embeddings fail, exact follow-up command, and no hidden failures. Define one deterministic exit policy for partial failure and enforce it in tests for both text and JSON modes.</action>
  <verify>
    <automated>uv run pytest tests/cli/test_kb_command.py -q -k "embed or partial or warning or json"</automated>
  </verify>
  <done>Users can immediately see whether embeddings are fresh and how to recover when they are not.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kb/test_qmd_backend.py tests/kb/test_qmd_adapter.py -q -k "embed"` passes.
- `uv run pytest tests/runtime/test_kb_service.py -q -k "embed or auto_embeddings"` passes.
- `uv run pytest tests/cli/test_kb_command.py -q -k "embed or partial or warning"` passes.
</verification>

<success_criteria>
- Aurora runs embeddings automatically after real KB mutations and never blindly on unchanged runs.
- Command completion is blocking with explicit embed stage status.
- Partial failures are visible, actionable, and regression-protected in both text and JSON outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd/02.1-02-SUMMARY.md`
</output>
