---
phase: 02.1-the-aurora-kb-suit-needs-working-it-need-to-be-easier-to-configure-update-delete-collections-and-ir-need-to-generate-embeddings-automatically-without-qmd
plan: 02
subsystem: kb
tags: [qmd, embeddings, cli, service, contracts, pytest]
requires:
  - phase: 02.1-01
    provides: KB config surface and persisted auto-embeddings setting
provides:
  - Typed embedding-stage contract in KB operation summaries
  - Delta-gated blocking embed orchestration in KB service lifecycle
  - CLI text/json embed-stage status rendering with deterministic partial-failure exit policy
affects: [kb-service, kb-cli, qmd-backend, runtime-tests]
tech-stack:
  added: []
  patterns:
    - "Embed stage runs only when mutation actually changes indexed state"
    - "Partial failures are surfaced via summary metadata and warning-first CLI output"
key-files:
  created: []
  modified:
    - src/aurora/kb/contracts.py
    - src/aurora/kb/qmd_adapter.py
    - src/aurora/kb/qmd_backend.py
    - src/aurora/kb/service.py
    - src/aurora/cli/kb.py
    - tests/kb/test_qmd_backend.py
    - tests/kb/test_qmd_adapter.py
    - tests/runtime/test_kb_service.py
    - tests/cli/test_kb_command.py
key-decisions:
  - "Partial embedding failures now exit with code 2 after rendering full text/json summaries."
  - "No-delta apply operations are treated as non-mutating to prevent blind embed runs."
patterns-established:
  - "KB summaries always carry embedding metadata (attempted/ok/category/recovery) for renderer parity."
  - "Service appends embed diagnostics to summary diagnostics instead of raising mutation-fatal errors."
requirements-completed: [KB-02, KB-05]
duration: 6 min
completed: 2026-03-05
---

# Phase 02.1 Plan 02: Auto-Embedding Orchestration Summary

**Automatic, delta-gated KB embedding orchestration with explicit partial-failure diagnostics and CLI recovery signaling.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-05T14:06:32Z
- **Completed:** 2026-03-05T14:12:34Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments
- Added typed embedding-stage metadata to KB contracts and exposed explicit backend `embed` transport.
- Implemented blocking embed orchestration in KB service, gated by real mutation delta and auto-embedding setting.
- Updated KB CLI output to surface embed status/warnings in text and JSON, with deterministic exit code for partial failures.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend contracts/backend protocol for embedding stage**
   - `9055207` (test): failing embed contract tests
   - `82df86a` (feat): typed embed contracts + backend/embed transport
2. **Task 2: Implement delta-gated blocking auto-embedding in service**
   - `76d4aff` (test): failing auto-embedding orchestration tests
   - `c966c19` (feat): service orchestration + partial-failure summary behavior
3. **Task 3: Render embed stage status + recovery in CLI outputs**
   - `9876b54` (test): failing CLI embed rendering/exit-policy tests
   - `32ebfd5` (feat): text/json embed-stage rendering + exit code 2 policy

## Files Created/Modified
- `src/aurora/kb/contracts.py` - Added `KBEmbeddingStageStatus` and embedding metadata support in operation summaries.
- `src/aurora/kb/qmd_adapter.py` - Added backend embed protocol path and fixed no-delta mutation flag behavior.
- `src/aurora/kb/qmd_backend.py` - Implemented `qmd embed` execution with privacy-safe typed diagnostics.
- `src/aurora/kb/service.py` - Added blocking embed stage orchestration and partial-failure propagation.
- `src/aurora/cli/kb.py` - Added embed-stage renderer + deterministic partial-failure exit code handling.
- `tests/kb/test_qmd_backend.py` - Added embed command transport/failure mapping coverage.
- `tests/kb/test_qmd_adapter.py` - Added adapter embed mapping and embedding summary serialization tests.
- `tests/runtime/test_kb_service.py` - Added delta/no-delta/dry-run/partial-failure service embed tests.
- `tests/cli/test_kb_command.py` - Added text/json embed visibility and exit policy regression tests.

## Decisions Made
- Partial embed failures are treated as non-fatal mutation outcomes in service, but surfaced as operational warnings in CLI with exit code `2`.
- Embed stage is never executed for dry-run or no-delta paths, even when auto-embeddings is enabled.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Prevented no-delta updates from being marked as state mutations**
- **Found during:** Task 2 (delta-gated orchestration)
- **Issue:** `QMDAdapter.apply_delta` returned `state_mutated=True` even when `applied` and `removed` were empty.
- **Fix:** Return `state_mutated=False` and skip manifest write for no-op apply deltas.
- **Files modified:** `src/aurora/kb/qmd_adapter.py`
- **Verification:** `uv run pytest tests/runtime/test_kb_service.py -q -k "embed or auto_embeddings or partial"`
- **Committed in:** `c966c19`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Required for correctness of delta-gated embed execution; no scope creep.

## Issues Encountered
- Execution tooling path expected under `~/.claude/get-shit-done`, but environment provides `~/.codex/get-shit-done`; commands were rerouted with no behavior impact.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Ready for `02.1-03-PLAN.md` (scheduler lifecycle + lock coordination).
- Embed-stage contracts are now stable for scheduler-triggered update/reporting reuse.

## Self-Check: PASSED
- Verified summary file exists on disk.
- Verified all task commits exist in git history (`9055207`, `82df86a`, `76d4aff`, `c966c19`, `9876b54`, `32ebfd5`).
